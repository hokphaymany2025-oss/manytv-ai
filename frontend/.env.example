# Base URL of the ManyTV backend. Defaults to http://127.0.0.1:8000 in
# src/api.ts if this isn't set, so this file is only needed if you're
# running the backend on a different host/port.
VITE_API_BASE_URL=http://127.0.0.1:8000
