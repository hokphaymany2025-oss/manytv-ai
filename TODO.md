# ManyTV — TODO

Generated from a full-repo analysis on 2026-07-17. See `PROJECT_ANALYSIS.md` for the full reasoning behind each item. Priority key: **P0** = broken/production-blocking, **P1** = should fix soon, **P2** = worth doing, **P3** = nice to have.

---

## v1.2 Planning (2026-07-18) — architecture review, current limitations, roadmap

Analysis-only pass, no application code changed. Baseline: tag `v1.1.1-ci-fix` (`b1c9981`, first-ever green GitHub Actions run — see the CI-fix Session Log entry). Done on a new `feature/v1.2-development` branch, which also carries one small commit not yet reflected elsewhere in this file: `pytest.ini` (`pythonpath = .`), which closes the "bare `pytest` still needs `-m`" residual gap named in the CI-fix entry — `python -m pytest` (in `ci.yml`) and `pythonpath = .` (in `pytest.ini`) now both independently put the repo root on `sys.path`, which is redundant but harmless; not worth un-doing either.

### Architecture review (current state)

**Backend** (~2000 lines Python across `backend/`, excluding tests): a FastAPI app that never loads a model itself — it orchestrates a locally running ComfyUI (HTTP+WS) and Ollama (OpenAI-compat HTTP) instance, both sharing one 8GB GPU.
- `backend/app.py` (78 lines) — entrypoint; lifespan starts the worker, health-checks ComfyUI, runs crash recovery, and drains the worker on shutdown. CORS is an explicit allowlist (empty by default) with narrowed methods/headers, not a wildcard.
- `backend/core/config.py` (80 lines) — one `Settings` object (`pydantic-settings`), every field has a safe default; nothing required at import time.
- `backend/core/comfyui_client.py` (153 lines) / `llm_client.py` (82 lines) — the two external-service clients. `comfyui_client.py` carries BUG-1's WS-drop→polling fallback.
- `backend/core/worker.py` (226 lines) — single-slot FIFO async queue shared by both job kinds (the primary anti-OOM guard); owns the `JobStatus`/`ShotStatus`/`LogLevel`/`LogStage` enums and `JobCancelled`.
- `backend/core/job_store.py` (344 lines) — persistence: one shared `sqlite3` connection (WAL, `threading.Lock`-guarded per BUG-6), three tables (`jobs`, `shots`, `job_logs`), publishes to an `EventBus` after every mutating write.
- `backend/core/events.py` (68 lines) — small in-memory SSE pub/sub; explicit, named single-process assumption.
- `backend/core/recovery.py` (146 lines) — startup reconciliation (resubmit-never-started vs. reuse-ComfyUI-history-for-already-submitted).
- `backend/core/gpu_memory.py` (33 lines) — best-effort VRAM cleanup, mostly a no-op safety net (ComfyUI, not this process, holds the model).
- `backend/api/routes/generate_script.py` (58 lines) — small, single-purpose.
- `backend/api/routes/storyboard.py` (**653 lines — by far the largest file in the codebase**, roughly a third of all backend code) — the naive shot-splitter, workflow-templating logic, the full job-execution handler, 8 HTTP routes (status/list/retry/cancel/download + 3 SSE streams), 3 SSE generator functions, and the artifact-metadata helper, all in one module. See Limitations below.
- `backend/models/schemas.py` (102 lines) — every Pydantic request/response model.

**Frontend** (~870 lines TS/TSX across `frontend/src/`, excluding tests): React 19 + Vite + TypeScript. No state management library, no CSS framework — plain `App.css` and local component state throughout, a deliberately dependency-light stack (only real runtime dependency: `react-router-dom`). Three data-fetching hooks (`useJob`, `useJobList`, `useJobLogs`) each own one `EventSource` connection since the SSE migration, each with a REST-based manual-refresh fallback retained. Two routed pages (`DashboardPage`, `JobDetailPage`); presentational components (`JobRow`, `JobTimeline`, `JobExecutionLogs`, `JobArtifacts`, `StatusFilter`, the two submit forms) each isolate any non-trivial logic into a pure, separately-tested function (`buildTimelineEvents`, `classifyArtifact`, `formatBytes`/`formatTime`).

**Tests:** 128 backend (`pytest`, calling route/generator functions directly rather than a real ASGI server in most cases — one exception needs a real `TestClient` for `Path(..., pattern=...)` validation), 53 frontend (`vitest`, hand-rolled fakes like `FakeEventSource` rather than a mocking library). Both 100% passing as of `v1.1.1-ci-fix`.

**End-to-end data flow:** browser form → REST POST → `worker.submit()` enqueues → `JobStore.create_job()` persists + publishes → the single worker slot dequeues FIFO → (`generate_script`: one Ollama call) or (`storyboard`: per-shot ComfyUI submit + WS/poll wait + download) → every state change persisted + published → open `EventSource` connections wake, re-fetch, push to the browser. No polling left anywhere in the frontend as of the SSE migration.

**Deployment model:** single local machine, single user, no auth, `127.0.0.1`-bound, no containerization, no reverse proxy — a deliberate, user-confirmed scope decision (BUG-3), not an oversight or a gap.

### Current limitations

Already tracked elsewhere in this file (pointer only, not re-explained): no dependency lockfile (P2 — see below for why this now has a concrete recent example), no retention/pruning for `job_logs`/`jobs`/`shots` (P3, and `output/` is already 13MB across 7 real job directories and growing), un-cached per-artifact `stat()` on every poll (P3), BUG-5's duplicate `ComfyUIClient` instances (P3, cosmetic), no retry/cancel attempt history (P3), the four SSE informational trade-offs (single-process `EventBus`, browser's 6-connection cap, no custom `EventSource` headers, the 404-signaling compromise), no request size limits (P3), and `generate_script`'s always-full-rerun-on-resume asymmetry (informational, permanent, not a gap).

Newly identified this session:
- **CI never runs the frontend at all.** `.github/workflows/ci.yml` only runs `pytest`. `npm run test` (53 passing), `npm run build`, and `npm run lint` (`oxlint`) all exist and all pass locally every session, but none are enforced in CI — a frontend regression wouldn't be caught until the next live-validation pass, if at all.
- **`project_id` is collected and persisted but never surfaced anywhere.** Both submission forms (`ScriptForm`, `StoryboardForm`) collect it, every job response includes it (`types.ts`, schemas) — but there's no dashboard filter or grouping by it. It's the one field in the whole data model that's write-only today.
- **`storyboard.py` (653 lines) is doing four jobs at once**: route handlers, core business logic (shot-splitting/templating), SSE streaming, and artifact metadata. The only file in the codebase with this kind of breadth, and the one most sessions have had to re-read most carefully before touching.
- **README's documented architecture tree is stale** — it predates `job_store.py`, `recovery.py`, `events.py`, `llm_client.py`, `frontend/`, `tests/`, and now `pytest.ini`; written at the very first milestone and never updated across the 10+ milestones since.
- ~~**No code coverage measurement on either side.**~~ **Backend done** (2026-07-19, `pytest-cov`) — see "Completed" below. Frontend (`vitest --coverage`) still open.
- **A `RUNNING` `generate_script` job still can't be cancelled at all** (Cancel silently has no effect for that one job type mid-run — already named as a scope limit under "Missing features," re-surfaced here since it's a real, visible UX gap, not just a code-level note).
- **ComfyUI cancellation is cooperative only, never `/interrupt`-based** — a cancelled job's in-flight shot still finishes generating before cancellation takes effect (same visibility note as above).

### v1.2 roadmap (proposed phases — not yet approved, no work started)

**Phase 1 — close the gaps this session's own CI investigation exposed** (small, low-risk, high-confidence): **done**
- ~~Add a frontend job to `.github/workflows/ci.yml`~~ — done (`cb84b1c`, "Add frontend CI validation").
- ~~Add a dependency lockfile~~ — done (`cef06c7`, "Add backend dependency lockfile").
- (Optional, cheap) Refresh README's architecture tree to match the current file layout — still open, not blocking.

**Phase 2 — make existing, currently-invisible data useful:**
- ~~Surface `project_id` in the dashboard~~ — **display done** (2026-07-18): `JobRow.tsx` now renders `project_id` as a badge when present, so it's visible on both the dashboard list and the job detail page (both reuse `JobRow`). A dedicated project **filter** mirroring `StatusFilter.tsx`'s pattern (and/or grouping) was intentionally out of scope for this pass and remains open if wanted later.
- ~~Retry/cancel attempt history~~ — **done, backend + frontend** (2026-07-19). Turned out to be narrower than originally framed here — see "Completed" below.

**Phase 3 — code health:**
- ~~Split `storyboard.py` along its four responsibilities~~ — done (2026-07-18). See "Completed" below for the module layout.
- ~~Add coverage measurement~~ — **done, both sides** (backend 2026-07-19 via `pytest-cov`, 91.27%; frontend 2026-07-19 via `@vitest/coverage-v8`, 66.94% — see the Completed entries below). Surfacing either in CI is a separate, still-open step.

**Phase 4 — deeper capability, needs explicit scope confirmation before any design work** (not recommended to start without a decision, given BUG-3's already-confirmed localhost-only scope):
- True ComfyUI `/interrupt` support for immediate (not just between-shot) cancellation.
- `generate_script` cancellation mid-run.
- A real job/log retention policy, now that real accumulated data exists (13MB+ and growing).
- Anything multi-user/remote-access related — **not recommended** unless the user explicitly revisits the localhost-only decision from BUG-3; named here only so it isn't silently forgotten as a future option.

### Recommended next features (concrete, prioritized)

1. ~~**Frontend CI job**~~ — done (`cb84b1c`).
2. ~~**Dependency lockfile**~~ — done (`cef06c7`).
3. ~~**`project_id` dashboard display**~~ — done (2026-07-18, `JobRow.tsx` badge). The filter/grouping half of the original idea is still open, not done — see Phase 2 above.
4. ~~**`storyboard.py` refactor**~~ — done (2026-07-18). See "Completed" below.
5. ~~**Retry/cancel attempt history**~~ — done, backend + frontend (2026-07-19). See "Completed" below.
6. ~~**Backend coverage measurement**~~ — done (2026-07-19, `pytest-cov`, raised from 85% to 91.27%). See "Completed" below.
7. ~~**Frontend coverage measurement**~~ — done (2026-07-19, `@vitest/coverage-v8`, 66.94% overall). See "Completed" below.

Recommendation: v1.2's roadmap is now fully closed except the two named, deliberately-deferred optional items (README refresh, wiring either coverage report into CI) and Phase 4 (gated on revisiting BUG-3's scope). `storyboard.py`'s specific low-coverage spots (see the Completed entries below) and the frontend's newly-quantified gaps (page-level and form components have no render tests at all) are both informational, not a to-do list on their own — raising either number further is a separate, not-yet-requested task.

---

## Completed [x]

- [x] FastAPI backend skeleton with lifespan-managed startup/shutdown (`backend/app.py`)
- [x] Env-driven settings via `pydantic-settings`, `.env.example` kept in sync with `Settings` defaults (`backend/core/config.py`)
- [x] ComfyUI HTTP + WebSocket client — queue prompt, stream progress, fetch history, download outputs (`backend/core/comfyui_client.py`)
- [x] Single-slot FIFO job worker shared by both job kinds, to avoid two processes contending for one 8GB VRAM budget (`backend/core/worker.py`)
- [x] `/api/generate-script` — LLM-backed script generation via Ollama's OpenAI-compat shim, including defensive `<think>` block stripping for Qwen3 (`backend/core/llm_client.py`, `backend/api/routes/generate_script.py`)
- [x] `/api/storyboard` — script → shots → per-shot ComfyUI generation → downloaded output files (`backend/api/routes/storyboard.py`)
- [x] `GET /api/jobs/{id}` polling endpoint, `GET /health` liveness check
- [x] Naive shot-splitting (one shot per non-empty script line) with an escape hatch for explicit shot lists
- [x] Workflow templating by `CLIPTextEncode` node title (`Positive`/`Negative`), documented in `backend/workflows/README.md`
- [x] Shipped, verified-working default workflow: SD1.5 + AnimateDiff v3 text-to-video (`backend/workflows/default_t2v.json`)
- [x] `scripts/run_comfyui.ps1` — launches ComfyUI with Dynamic-VRAM-correct flags, checked against the actual installed `cli_args.py` rather than assumed
- [x] Anti-OOM safeguards: single-slot concurrency, `MAX_SHOTS_PER_JOB`, `GENERATION_TIMEOUT_SECONDS`, `--vram-headroom`
- [x] **Real end-to-end run verified on target hardware** (Intel Arc A750): shot 0 of job `fb4850f6-17c8-4798-881f-6321b45413b5` generated a valid `.mp4` — see `output/fb4850f6-17c8-4798-881f-6321b45413b5/shot_000/ManyTV_00002.mp4` and `server.log`
- [x] README.md — accurate, verified-on-this-machine setup and architecture documentation
- [x] Repo is now under version control (`git init` + baseline commit, 2026-07-17)
- [x] **BUG-2 fixed** (2026-07-17): `StoryboardRequest.workflow_name` now constrained to `^[A-Za-z0-9_-]+$` via Pydantic `Field(pattern=...)` in `backend/models/schemas.py`, closing the path-traversal input. See "Bugs" below for detail, `SESSION_STATE.md` Session Log for the full record.
- [x] Test tooling bootstrapped: `pytest` added to `requirements.txt`, first test module `tests/test_schemas.py` (11 cases covering the BUG-2 fix), all passing.
- [x] **BUG-1 mitigated** (2026-07-17): `ComfyUIClient.wait_for_completion` now falls back to polling `/history` instead of failing the job outright when the monitoring WebSocket drops. Unit-tested with mocks (`tests/test_comfyui_client.py`, 4 new cases).
- [x] **BUG-1 CLOSED — live-validated against real ComfyUI** (2026-07-17): forced a real WebSocket drop mid-generation against a live ComfyUI instance (Arc A750, XPU backend) and confirmed the fallback engages and the job still completes. Full evidence in "Bugs" below and `SESSION_STATE.md` Session Log.
- [x] **BUG-3 fixed** (2026-07-17): CORS now defaults to an empty allowlist (`CORS_ALLOWED_ORIGINS` env var, `backend/core/config.py`) instead of `allow_origins=["*"]`; `allow_methods`/`allow_headers` narrowed to only what's used. Scope confirmed with the user first: localhost-only single-user deployment, so no auth layer was needed — see "Bugs" below for full detail.
- [x] **CI added** (2026-07-17): `.github/workflows/ci.yml` runs the full `pytest` suite on push/PR to `master` (`ubuntu-latest`, Python 3.12, pip-cached). Along the way, split the Intel Arc/XPU `torch` stack out of `requirements.txt` into a new optional `requirements-gpu.txt` — it wasn't needed by anything the tests touch (confirmed by grep: only `backend/core/gpu_memory.py` imports `torch`, defensively, and the actual working dev venv never had it installed either), and installing it in CI would have meant downloading a multi-GB, hardware-specific stack for no reason. Design (runner OS, requirements split vs. inline `grep -v` workaround) was proposed to the user with explicit tradeoffs before implementing; user approved the recommended defaults. Dry-run-validated locally: a from-scratch venv installing only the new lean `requirements.txt` (no torch present) ran all 35 tests clean in ~12s. **Not yet observed running as a real GitHub Actions job** — this repo has no git remote configured, so the workflow can't execute anywhere until pushed to a GitHub-hosted repo; that's the one thing this session couldn't validate live.
- [x] **Job Manager + Queue + Resume System milestone (2026-07-17).** Job/shot state is now persisted to SQLite (`backend/core/job_store.py`, `output/jobs.db`) and survives a backend restart. A new `backend/core/recovery.py` reconciles incomplete jobs at startup — resubmitting never-started shots, and for shots already submitted to ComfyUI before a crash, checking `/history` first and reusing the result rather than blindly resubmitting (a real ComfyUI workflow isn't guaranteed deterministic, so resubmitting could silently produce a different video than one already relied on). **Live-validated end-to-end**, not just unit-tested: a job was killed mid-shot-1 (backend process only, ComfyUI untouched) and, on restart, correctly found shot 1's existing ComfyUI history and reused it instead of resubmitting, then completed shot 2 normally. Full evidence in `SESSION_STATE.md` Session Log. Design doc: `C:\Users\hokph\.claude\plans\warm-prancing-karp.md`.
  - This also **closes BUG-4** (partial job failure losing successful-shot results) as a natural side effect — `GET /api/jobs/{id}` now builds its response from the persisted `shots` table at read time, so completed shots are visible even while the job is still `RUNNING`, not only after full success. See BUG-4 below.
  - `project_id` (free-form, caller-supplied grouping tag, no backing entity) and `workflow_name` are now stored and returned on jobs; `JobStatusResponse` gained a `shots` field with per-shot `status`/`prompt_id`/`files`/`error`.
  - New test modules: `tests/test_job_store.py` (7 cases — CRUD, partial-update COALESCE semantics, shot upsert merging) and `tests/test_recovery.py` (10 cases — job-level routing, all three shot-reconciliation branches: DONE-skip, PENDING-normal, SUBMITTED×{history-found reuse, history-absent resubmit, ComfyUI-unreachable propagates without resubmitting}). `python -m pytest tests/ -v` → 32 passed.
- [x] **Job retry, job cancellation, and first frontend milestone (2026-07-17).** Planned via Plan mode (design doc: `C:\Users\hokph\.claude\plans\logical-mapping-biscuit.md`), including a dedicated Plan-agent review that caught two real gaps before implementation (see BUG-6 below for a third, found only by live-testing).
  - **Retry** (`POST /api/jobs/{job_id}/retry`): reuses `_run_storyboard_job`'s existing DONE-skip/SUBMITTED-reconcile shot loop completely unmodified — a retried job just re-runs through the same code path a fresh job does. New `JobStore.reset_job_for_retry`/`reset_shots_by_status` methods explicitly clear stale `error`/`result` (impossible via `update_job_status`'s COALESCE semantics). Rejects a job cancelled while still `QUEUED` (`started_at IS NULL`) since it never created shot rows or consumed its queue entry — retrying it would risk a duplicate queue entry for the same job id; told to submit fresh instead.
  - **Cancellation** (`POST /api/jobs/{job_id}/cancel`): new `JobStatus.CANCELLING` (signal)/`CANCELLED` (terminal) states and a `JobCancelled` exception. A `QUEUED` job is cancelled instantly; a `RUNNING`/`RESUMING` storyboard job is cooperatively stopped between shots (checked at the top of every shot-loop iteration, including DONE-skipped ones) — deliberately **not** mid-generation (no ComfyUI `/interrupt` call; unverifiable without a live ComfyUI instance this session, so left as a documented scope limit rather than built on an assumption). A running `generate_script` job can't be cancelled at all (no per-chunk checkpoint inside its one `await`), only a queued one. Fixed a real race in `backend/core/recovery.py::_resume_comfyui_jobs` the Plan-agent review caught: it captured job rows once before a possibly-indefinite ComfyUI-health wait and would've silently clobbered a cancellation requested during that wait — now re-fetches fresh before acting, and `resume_incomplete_jobs`'s startup query includes `CANCELLING` so a stale cancel-in-flight-at-crash-time is finalized rather than resubmitted.
  - **Frontend** (`frontend/`): React + Vite + TypeScript, first real milestone since this project's inception. Minimal job dashboard — submit script/storyboard jobs, live-polling job list (client-tracked job ids via `localStorage`, no new list-jobs endpoint — deliberately out of scope), retry/cancel buttons, download finished files. New narrow `GET /api/jobs/{job_id}/files/{shot_index}/{filename}` route (reusing BUG-2's exact `Field(pattern=...)` path-traversal-proof technique) rather than a blanket `StaticFiles` mount, since `Settings.db_path` defaults to living inside `output/` — mounting the whole directory would have made `jobs.db` itself fetchable. Node.js LTS installed via `winget` (wasn't present on this machine) with the user's explicit approval first.
  - New/updated test modules: `tests/test_worker.py` (new, 9 cases — closes the pre-existing "worker has zero direct tests" gap), `tests/test_job_routes.py` (new, 17 cases — retry/cancel/download route logic), `tests/test_job_store.py` (+5 cases including a concurrency regression test), `tests/test_recovery.py` (+6 cases for the CANCELLING race fix and shot-loop cooperative cancellation). `python -m pytest tests/ -v` → 72 passed.
  - **Live-validated, not just unit-tested**: real backend + real frontend dev server driven with Playwright (headless Chromium; `chromium-cli` wasn't available in this environment) — submitted real jobs through the actual UI, confirmed the job list live-polls to terminal states, confirmed the Retry button appears on a real `failed` job and successfully re-queues it, confirmed Cancel works on a real queued job, screenshotted throughout. Also independently confirmed via direct `curl` against the running backend: cancel-while-queued, retry-after-ComfyUI-unreachable-failure (re-fails for the identical real reason on retry, confirming state was genuinely reset and re-run, not faked), cancel-on-terminal-job → 409, download-missing-file → 404, and a real CORS preflight from the frontend's actual origin.
- [x] **BUG-6 fixed** (2026-07-17), found only by the live frontend test above: a real, pre-existing `sqlite3.InterfaceError: bad parameter or other API misuse` under genuine concurrent access to `JobStore` — see "Bugs" below for full detail. Not something any mocked unit test could have caught; this is exactly why the live Playwright pass was worth doing even after 71 mocked tests were green.
- [x] **List-jobs endpoint added (2026-07-17):** `GET /api/jobs` — full job history, newest first, optional `?status=failed,cancelled`-style comma-separated filter. `JobStore.list_jobs(status_in=...)` extended to treat an omitted/empty filter as "no filter, everything" (existing callers — `recovery.py` — always pass an explicit filter, so this is purely additive) and to `ORDER BY created_at DESC`. `_build_job_status_response` (already shared by `get_job_status`/`retry_job`/`cancel_job`) gained an optional pre-fetched-row parameter so the list route avoids an N+1 re-fetch per job. New tests: `tests/test_job_store.py` (+2 — no-filter-returns-everything, ordering) and `tests/test_job_routes.py` (+4 — unfiltered, comma-filtered, empty-list, shots-included-for-storyboard-jobs). `python -m pytest tests/ -v` → 78 passed. **Live-validated** against a real backend holding genuine multi-session history (20 real jobs accumulated across this and prior sessions' live tests): unfiltered call returned all 20 newest-first, `?status=failed` correctly returned exactly the 6 real failed jobs. Frontend not yet updated to use this (still client-tracks via `localStorage`) — that's the natural next follow-up, not done in this pass to keep scope to what was asked.
- [x] **Frontend wired to `GET /api/jobs` (2026-07-17):** replaced (not supplemented) the `localStorage`-based job-id tracking with real server-side history. `frontend/src/api.ts` gained `listJobs(status?)`; new `frontend/src/useJobList.ts` polls it every 3s and also refreshes immediately right after a job is submitted (so it appears without waiting for the next tick); `useTrackedJobs.ts` deleted entirely, not left as dead code. `ScriptForm`/`StoryboardForm`'s `onSubmitted` prop simplified from `(jobId: string) => void` to `() => void` since the id is no longer needed by the caller. **First automated frontend tests**: added `vitest` + `@testing-library/react` + `jsdom` (config folded into the existing `vite.config.ts`, no second config file), `frontend/src/api.test.ts` (3 cases) and `frontend/src/useJobList.test.tsx` (4 cases — initial fetch, interval re-fetch, in-place `updateJob`, resilience to a failed poll) — `npm run test` → 7 passed. `npm run build` still compiles clean. **Live-validated** with two independent Playwright browser contexts (separate `localStorage`, standing in for two different browsers/machines) against a real running backend: browser A submitted a job, and browser B — which never submitted anything itself — showed that exact same job id within one poll interval, purely from server-side history. Zero console errors on a plain page load.
- [x] **Status-filter UI added to the dashboard (2026-07-17):** wired the backend's already-existing `?status=` filter (from the list-jobs-endpoint session) to a real control — `useJobList.ts` gained `status`/`setStatus` state; `refresh` now depends on `status` and calls `listJobs(status || undefined)`, so changing the filter naturally re-triggers the existing mount/interval effect (fetch immediately with the new filter, then keep polling with it — no separate wiring needed). New `frontend/src/components/StatusFilter.tsx` — a `<select>` with "All" plus the 7 known job statuses. Also fixed a stale UX string noticed while touching this: `JobList.tsx`'s empty-state message still said "No jobs submitted from this browser yet." (leftover from the deleted `localStorage`-tracking design) — now distinguishes "No jobs yet." from "No jobs match this filter." depending on whether a filter is active. Planned via Plan mode first (architecture/design/risks reviewed and approved before any code, per the user's explicit request) — design doc: `C:\Users\hokph\.claude\plans\logical-mapping-biscuit.md`. Tests: `useJobList.test.tsx` +3 cases (filter triggers refetch with the right arg, switching back to "All" refetches with no filter, the poll interval keeps using whatever filter is currently active) — `npm run test` → 10 passed. `npm run build` clean. **Live-validated** against the real backend's actual accumulated history (24 real jobs, 6 of them failed): selecting "Failed" in the live UI correctly narrowed the list from 24 to exactly 6, all shown rows read `failed`, switching back to "All" restored all 24; selecting a status with zero real matches (`resuming`) correctly showed "No jobs match this filter." Zero console errors throughout.
- [x] **Job Timeline added (2026-07-18):** closed the timestamps gap the Job Detail page milestone deliberately deferred. Planned via Plan mode first (architecture/design/risks reviewed and approved before any code) with two `AskUserQuestion` forks resolved directly: **current-attempt lifecycle only** (retrying/cancelling a job clears `started_at`/`finished_at`, so a past attempt before a retry leaves no trace — building full retry/cancel history would need a new append-only events table touching every state-transition call site, out of scope here) and **no new date library** (a small local `formatTime.ts` using native `Date`/`Intl`, matching this frontend's zero-extra-dependency style). Turned out to be a pure exposure gap, not a data gap: `JobStore` already persisted all six timestamps and `_build_job_status_response` already read them into its row dicts, just never copied them into the response models.
  - **Backend:** `backend/models/schemas.py` — `JobStatusResponse` gained `created_at`/`started_at`/`finished_at`, `ShotStatusResponse` gained `created_at`/`submitted_at`/`finished_at` (all `Optional[float]`). `backend/api/routes/storyboard.py::_build_job_status_response` (shared by `GET /api/jobs/{id}`, `GET /api/jobs`, retry, cancel) now passes these through — all four routes gained timestamps for free, no new routes or migration. Tests: `tests/test_job_routes.py` +2 (job-level and shot-level timestamp round-trip through `get_job_status`). `python -m pytest tests/ -v` → **80 passed**.
  - **Frontend:** new `frontend/src/formatTime.ts` (`formatAbsolute`/`formatRelative`, native `Date`/`Intl` only) and `frontend/src/components/JobTimeline.tsx` — exports a pure `buildTimelineEvents(job)` (tested in isolation, same convention as this project's other logic-not-JSX tests) plus the rendering component. Events: job created → job started → per-shot submitted/finished (storyboard only, interleaved chronologically) → job terminal status. Null timestamps omitted; deliberately excludes `shot.created_at` (every shot bootstraps in the same synchronous loop right after the job starts, so it would just duplicate "Job started"). Rendered on `JobDetailPage.tsx` below the existing `<JobRow>`. `frontend/src/types.ts` mirrors the six new fields; three existing test fixtures (`api.test.ts`, `useJob.test.tsx`, `useJobList.test.tsx`) updated since the new fields are non-optional on the TS interface. Tests: `formatTime.test.ts` (6 cases) + `JobTimeline.test.ts` (6 cases, including the interleaving case) — `npm run test` → **27 passed** (15 prior + 12 new). `npm run build` clean.
  - **Live-validated:** real backend + real Vite dev server, driven with Playwright. Submitted a `generate_script` job — timeline showed only "Job created" while queued, then "Job created → Job started → Job done" on completion with correct absolute/relative times. Submitted a `storyboard` job with ComfyUI intentionally not running — timeline showed "Job created → Job started → Job failed", the real ComfyUI-unreachable error surfaced, Retry button present. Zero console errors in both runs. Per-shot submitted/finished interleaving with a real ComfyUI run wasn't exercised live (ComfyUI wasn't running this session) — covered instead by the backend round-trip test and `JobTimeline.test.ts`'s interleaving case.
- [x] **Frontend polling replaced with Server-Sent Events (2026-07-18):** all three data-fetching hooks (`useJob`, `useJobList`, `useJobLogs`) previously ran independent `setInterval(refresh, 3000)` loops — up to three separate fetches every 3s per open tab regardless of whether anything changed, and 0-3s latency on any real update. Planned via Plan mode with an 11-point design brief (polling inventory, event taxonomy, SSE-vs-WebSocket justification, backend/frontend architecture, reconnection, failure handling, testing, migration plan, risks) plus a dedicated Plan-agent design-review pass that caught two real correctness bugs before any code was written: **(a)** publishing from inside `JobStore._run()`'s thread-dispatched sync callable would touch `asyncio.Queue` off the event-loop thread — not safe, structurally the same class of bug as BUG-6 one layer up; **(b)** fetching an initial snapshot before subscribing (rather than after) would leave a gap where a change could fire and be silently lost, hanging a connection on stale data. Both corrected in the shipped design. A third, independent bug was found only by *this session's own* live `curl` testing, not planning or unit tests: `GET /api/jobs/events` was being swallowed by the pre-existing `GET /api/jobs/{job_id}` route (FastAPI matches routes in registration order; both have exactly two path segments, so `{job_id}` matched first with `job_id="events"` and 404'd) — fixed by moving the more specific route ahead of the parameterized one, exactly the kind of thing this project's live-validation discipline exists to catch.
  - **Backend:** new `backend/core/events.py` — a small, lock-free, dependency-free `EventBus` (`asyncio.Queue` per subscriber, bounded, drop-oldest). `JobStore` gained an *optional* `event_bus` param (defaults to a private instance, not the global singleton, so every existing test's `JobStore(Settings(...))` call site stays isolated) and a `_run_and_publish` wrapper that all six state-mutating methods now go through — publishing only *after* the threaded sqlite call returns, back on the event-loop thread (the fix for bug (a) above). Three new SSE routes in `storyboard.py`: `GET /jobs/{job_id}/events` (full `JobStatusResponse` snapshot on every change, reusing `_build_job_status_response` unchanged), `GET /jobs/{job_id}/logs/events` (delta-only `JobLogEntry` pushes, with real `Last-Event-ID` reconnect support using `job_logs.id`'s already-durable autoincrement), and `GET /jobs/events` (a deliberately signal-only channel for the dashboard list — discovered during implementation, not planning, that pushing per-job snapshots there can't correctly express a job *leaving* a status-filtered view, since the frontend's upsert-by-id helper can only add/replace, never remove; signaling "something changed" and letting the client re-run its own already-correct, already-filtered `GET /api/jobs` fetch sidesteps that entirely). `backend/app.py`'s `CORSMiddleware` gained `Last-Event-ID` in `allow_headers` (this frontend is genuinely cross-origin — Vite `:5173` vs backend `:8000` — and that header isn't CORS-safelisted; without this, reconnects would silently fail preflight while first connects wouldn't). All three existing polling routes (`GET /api/jobs`, `GET /api/jobs/{id}`, `GET /api/jobs/{id}/logs`) were left completely untouched — kept indefinitely, not removed, satisfying "preserve the existing API." Tests: 15 new (7 `EventBus` unit tests, 8 SSE-route generator tests — calling the generator functions directly with a duck-typed fake request, matching this repo's existing "call route functions directly" convention rather than a real streaming `TestClient`). `python -m pytest tests/ -v` → **114 passed**.
  - **Frontend:** zero new dependencies — `EventSource` is a native browser API. Each hook kept its exact external return shape, so **zero consuming components changed** (`JobRow`, `JobTimeline`, `JobExecutionLogs`, `JobArtifacts`, `DashboardPage`, `JobDetailPage` are all untouched). `useJobLogs` appends each delta `log_added` event. `useJob` keeps a one-shot `getJob` existence check before opening its stream (preserves the exact `notFound`/`ApiError.status===404` behavior, since `EventSource` can't expose an HTTP status once a stream begins) via a `cancelled`-flag-guarded async effect that avoids a real race (opening a stale connection for a `jobId` that already changed again before the initial check resolved). `useJobList` opens *one* persistent connection for the component's whole lifetime, independent of the status filter — a `refreshRef` indirection lets every incoming signal call whichever `refresh()` is current (respecting whatever filter is active) without needing to close/reopen the stream on every filter change. `refresh()` is kept on all three hooks as a real, working manual re-fetch via the pre-existing REST calls, not removed. Tests: new `FakeEventSource` test doubles (hand-rolled, matching this frontend's "no mocking library" convention) in each hook's test file plus `JobExecutionLogs.test.tsx`. `npm run test` → **53 passed** (49 prior + 4 net new, after accounting for interval-polling tests replaced by SSE-equivalent ones). `npm run build` clean.
  - **Live-validated, including a real bug caught only by it:** confirmed via direct `curl` that all three SSE routes produce correct output, including `Last-Event-ID` correctly skipping already-seen log rows on a simulated reconnect. Via real backend + real Vite dev server + Playwright: a newly submitted job appeared in the dashboard list in under 1 second (vs. the old 3s poll ceiling); a real `generate_script` job's status/generated-script text/execution log/timeline all updated live end-to-end (95s real Ollama run) with **zero page reloads and zero console errors**. **Reconnection**: killed the real backend process while a browser tab was open, confirmed the still-open tab's `EventSource` auto-reconnected (observable via `net::ERR_CONNECTION_RESET`/`ERR_CONNECTION_REFUSED` console entries during the actual outage window — the correct, expected signature of automatic retry attempts, not application bugs), restarted the backend, submitted a new job via `curl`, and confirmed the same still-open tab picked it up with zero manual interaction or reload.
- [x] **Job Output Artifacts added (2026-07-18):** the raw local filesystem path was, until now, sent straight through the JSON API (`ShotStatusResponse.files: list[str]`, e.g. `"output\\<job_id>\\shot_000\\ManyTV_00009.mp4"`), which the frontend then regex-stripped client-side just to get a displayable filename. This feature fixed that exposure and added real value: per-artifact size/content-type, and inline `<video>`/`<img>` previews on the Job Detail page. Planned via Plan mode with a dedicated Plan-agent design review; two decisions confirmed directly with the user: **metadata computed at read time** (file size via `stat()`, content-type guessed from the extension) rather than persisted — no schema change to `shots.files`'s stored shape, no migration of existing `jobs.db` rows — and **inline preview + metadata**, not just enriched download links, since this is a video-generation tool where seeing the output without downloading first is high-value.
  - **Backend:** `backend/models/schemas.py` gained `ArtifactResponse` (`filename`, `size_bytes`, `content_type`); `ShotStatusResponse.files` changed from `list[str]` to `list[ArtifactResponse]` (a replace, not a supplement — no dual-shape transition period, matching every prior milestone's practice). New `_artifact_from_path` helper in `backend/api/routes/storyboard.py`, wired into `_build_job_status_response`'s shot loop. Its `try/except OSError` around only the `.stat()` call is deliberate, justified handling (not reflexive defensive coding this codebase otherwise avoids): this is a confirmed single-user localhost deployment with unmediated filesystem access to `output/`, so a file genuinely can disappear between being recorded and being read on a later 3s poll — an unhandled `FileNotFoundError` here would turn one deleted file into a permanently-broken job status response, not a one-time failure. `download_shot_file` gained an explicit `media_type=` (additive, not a behavior change — Starlette's implicit guess already produced the same value) so the JSON `content_type` and the real HTTP header can never silently diverge later. Tests: 5 new cases (`_artifact_from_path` for a known extension, an unknown extension, and a missing file; `get_job_status` end-to-end for a real file and a missing one) — `python -m pytest tests/ -v` → **99 passed**.
  - **Frontend:** no new hook needed — `job.shots[*].files` was already present in the `JobStatusResponse` that `useJob`/`useJobList` already poll, unlike Job Execution Logs, which needed a dedicated endpoint. New `frontend/src/formatBytes.ts` (sibling to `formatTime.ts`, same native-only convention) and `frontend/src/components/JobArtifacts.tsx` — exports a pure `classifyArtifact(contentType)` (`video`/`image`/`other`, tested in isolation, matching `JobTimeline`'s convention) plus the render component: `video/*` → `<video controls>`, `image/*` → `<img>`, anything else → download link only, each with an `onError` fallback to a plain link if a preview fails to load. `JobRow.tsx` needed a mandatory edit regardless of UX choice (its existing per-file rendering assumed `file: string`) — used the opportunity to also add a `showFiles` prop (default `true`) so `JobDetailPage.tsx` can suppress `JobRow`'s now-redundant per-file links (`showFiles={false}`) without touching the dashboard list view's rendering at all. `frontend/src/api.ts::downloadUrl` simplified now that the backend returns a clean basename directly. Tests: `formatBytes.test.ts` (7 cases) + `JobArtifacts.test.tsx` (8 cases, `classifyArtifact` plus rendering through the DOM like `JobExecutionLogs.test.tsx`) — `npm run test` → **49 passed** (34 prior + 15 new). `npm run build` clean.
  - **Live-validated against real data, not fabricated inputs, for the primary path**: this machine's `output/jobs.db` already held two real jobs with genuine ComfyUI-produced `.mp4` files from prior live sessions. Opening one's real Job Detail page rendered three real, correctly-sized inline `<video>` previews — confirmed actually decodable (`readyState: 4`, correct `videoWidth`/`videoHeight`), not just present in the DOM. For the image/other-content-type/missing-file branches (unreachable live without a real ComfyUI run, since `_run_storyboard_job`'s `health_check()` happens before any shot rows are created — confirmed by reading the code), used a throwaway script (scratchpad-only, same accepted pattern as BUG-1's throwaway TCP proxy) writing directly to the real `output/jobs.db` to fabricate one job with a real 1x1 PNG, a non-media file, and a shot pointing at a file that was never written — all three rendered correctly (a genuinely decoded image, a plain download link, and "unknown size" with no crash) — then deleted afterward, confirmed via `git status` and a directory listing that nothing fake was left behind. Zero console errors throughout.
- [x] **Job Execution Logs added (2026-07-18):** a curated, per-job narrative log — distinct from the Job Timeline's fixed state-transition timestamps, this is genuinely new persisted data (free-form message lines, unbounded cardinality per job) that didn't exist in the database at all before. Planned via Plan mode first, including a text-only intermediate analysis pass and a dedicated Plan-agent design review before implementation, with several decisions confirmed directly with the user: **SQLite** (a new table in `JobStore`, not the filesystem); schema is `timestamp, level, stage, message, shot_index` (plus `job_id` and a surrogate autoincrement `id` for stable ordering); **curated events only** (lifecycle transitions, warnings, errors — not the high-frequency per-WebSocket-progress-tick "step X/Y" line); **a separate `GET /api/jobs/{id}/logs` endpoint**, not embedded in `JobStatusResponse` (which the dashboard's `GET /api/jobs` list view also uses and has no need for full log history on every 3s poll); and — the one place this deliberately differs from Job Timeline's current-attempt-only scope — **logs persist across retries**, since seeing why a job failed last time is valuable, unlike the timestamps that `reset_job_for_retry` intentionally clears.
  - **Backend:** `backend/core/job_store.py` gained the `job_logs` table + `add_job_log`/`get_job_logs` (same lock-guarded `_run()` pattern as every other method, no new dependency; `add_job_log` accepts an optional explicit `timestamp` for callers that captured an event earlier than the call itself). New `LogLevel`/`LogStage` enums in `backend/core/worker.py` alongside `JobStatus`/`ShotStatus`. Explicit `add_job_log` calls added at ~12 existing call sites across `worker.py`, `storyboard.py` (`_run_storyboard_job`, `retry_job`, `cancel_job`), `recovery.py`, and `generate_script.py` — a plain method call next to each existing `logger.*` line, not a `logging.Handler` (rejected: `Handler.emit()` is sync while every `JobStore` write is `async def`; bridging that safely would need a queue/consumer this codebase has nothing like, and zero existing tests here touch the `logging` module at all). Two genuinely new call sites (the shot-failure `except` blocks in `_run_storyboard_job` had no `logger` call at all before — per-shot failures previously only surfaced as the job-level `"Job failed."` line). The one real architectural wrinkle: `comfyui_client.py`'s WS-drop-falls-back-to-polling warning (BUG-1) — `comfyui_client.py` stays fully job-agnostic (never touches `JobStore`, no signature change to the already-shipped, live-validated `wait_for_completion`/`on_progress` contract); the event rides the existing synchronous `on_progress` callback as a synthetic message, captured with its true timestamp by `storyboard.py`'s `_progress` closure into a small buffer, and drained (persisted) in a `finally` block after `wait_for_completion` resolves — avoiding both a signature change and a wrong persisted-timestamp ordering bug an earlier draft of this design would have introduced. New `JobLogEntry` schema; new `GET /api/jobs/{job_id}/logs` route (404 on a missing job, same convention as the other job routes). Tests: 14 new/extended cases across `test_job_store.py` (round-trip, same-timestamp tie-break by insertion order, scoping, and — the highest-value case — confirming `reset_job_for_retry`/`reset_shots_by_status` do *not* clear `job_logs`), `test_job_routes.py`, `test_worker.py`, `test_recovery.py`, and `test_comfyui_client.py` (confirms the WS-drop event is relayed through the still-synchronous `on_progress`). `python -m pytest tests/ -v` → **94 passed**.
  - **Frontend:** new `frontend/src/useJobLogs.ts` (matching `useJob`/`useJobList`'s 3s-poll shape, no separate 404-handling since it only ever mounts after `useJob` has confirmed the job exists) and `frontend/src/components/JobExecutionLogs.tsx`, rendered on `JobDetailPage.tsx` below the existing `<JobTimeline>`. Each row shows absolute/relative time (reusing `formatTime.ts`), a level badge, an optional "shot N" tag, and the message; level-based background coloring in `App.css` mirrors `job-row__status--{status}`'s existing convention. Honest scope note: unlike `JobTimeline`, there's no pure derivation function worth extracting here (the backend already returns logs pre-sorted), so `JobExecutionLogs.test.tsx` is this project's first test that renders through the actual DOM rather than testing isolated logic — and the first component test overall, which surfaced that `@testing-library/jest-dom` was never installed, so its assertions use plain `className`/`textContent` checks instead of `toHaveClass`/`toBeInTheDocument` rather than adding a new dependency. Tests: `api.test.ts` (+1), `useJobLogs.test.tsx` (new, 3 cases), `JobExecutionLogs.test.tsx` (new, 3 cases) — `npm run test` → **34 passed** (27 prior + 7 new). `npm run build` clean.
  - **Live-validated:** real backend + real Vite dev server, driven with Playwright. A `generate_script` job showed "Starting job" while in flight, then "generated script (N chars)"/"completed in N.Ns" once done. A `storyboard` job (ComfyUI intentionally not running) showed the job-level failure log entry with error-level red styling; retrying it and letting it fail again confirmed the one live check genuinely unique to this feature — **the execution log kept both attempts' entries together** (5 total: start → fail → retried → start → fail again), never cleared, exactly as designed. Zero console errors throughout. Shot-level queued/done lines, the two resume-reconciliation lines, and the WS-drop-fallback relay specifically weren't exercised live (ComfyUI wasn't running this session, so no shot ever got far enough to submit) — covered instead by the backend unit tests above, the same documented gap prior ComfyUI-dependent sessions have had.
- [x] **Test coverage extended to `storyboard.py`'s pure helper functions (2026-07-18):** `_naive_shot_split`/`_apply_shot_to_workflow` were previously only exercised indirectly through `_run_storyboard_job` integration-style tests — new `tests/test_storyboard_helpers.py` (14 cases) tests them directly: empty/whitespace-only scripts, blank lines skipped without leaving a gap in shot indices, sequential zero-based index assignment, per-line whitespace stripping, case-insensitive `CLIPTextEncode` title matching (`"POSITIVE"`, `"  Positive  "`, etc.), the documented "empty `negative_prompt` doesn't clobber the workflow's own default" behavior, non-`CLIPTextEncode`/unrecognized-title/missing-`_meta` nodes all correctly left untouched, and confirming `_apply_shot_to_workflow`'s deep-copy actually leaves the original workflow dict unmutated (not just a copy that happens to look unchanged — verified the *patched* copy's text really did change while the original's didn't). **All 14 passed against the existing implementation with zero production code changes needed** — every documented behavior held. `python -m pytest tests/ -v` → **128 passed**. Closes the last remaining item from `TODO.md`'s "Recommended next steps."
- [x] **Job Detail page added (2026-07-17):** first real routing in this frontend — added `react-router-dom` (confirmed with the user first: real URL-based routing over a no-dependency in-page toggle, and timestamps explicitly deferred to a later pass since `JobStatusResponse` doesn't expose them and this one didn't need backend changes). `App.tsx` became route definitions (`/` → new `pages/DashboardPage.tsx`, holding the old `App.tsx` body verbatim; `/jobs/:id` → new `pages/JobDetailPage.tsx`); `main.tsx` wraps in `<BrowserRouter>`. New `useJob.ts` (single-job counterpart to `useJobList.ts` — same 3s-poll shape, same "keep last-known state on a transient failure" resilience) additionally tracks `notFound`: a confirmed 404 stops issuing further polls (a job that doesn't exist will never start existing). `JobDetailPage` **reuses `<JobRow>` directly** (wrapped in `<ul className="job-list">` so the bare `<li>` doesn't show a bullet marker) rather than duplicating its status/error/script/shots/retry/cancel/download rendering — the detail page's value is the real per-job URL, not a different visual treatment. `JobRow.tsx` gained a `<Link to="/jobs/:id">` on the job id as the entry point. Small, well-justified `api.ts` addition: `request()` now throws a typed `ApiError` (carries `.status`) instead of a plain `Error`, so `useJob` can check `err.status === 404` as a real property rather than string-matching the message — first time this distinction was actually needed. Tests: `useJob.test.tsx` (new, 4 cases — initial fetch, interval re-fetch, transient-failure resilience, 404 stops further polling) and `api.test.ts` (+1, `ApiError.status` on a 404) — `npm run test` → 15 passed. `npm run build` clean. **Live-validated**: clicking a job's id from the dashboard navigates to `/jobs/<id>` (URL confirmed), browser back button returns to `/`, a **hard refresh while on `/jobs/<id>`** renders correctly (confirms Vite's dev server SPA-fallback works, the one risk in this feature that depended on tooling behavior rather than app code), navigating directly to a fabricated id shows "Job not found." instead of an infinite loading state, and clicking Retry on a real failed job from the detail page correctly flipped its status (`failed` → `running`), proving the reused `JobRow`/`useJob.updateJob` wiring works standalone.

- [x] **`project_id` exposed in the dashboard (2026-07-18, Phase 2.1):** backend already returned `project_id` on `JobStatusResponse` (Job Manager milestone) and the frontend already typed it (`types.ts`) and collected it in both submit forms — it was captured and persisted but never rendered anywhere. `JobRow.tsx` now renders `job.project_id` as a small badge in the row header (next to kind/id/status), only when non-null; new `.job-row__project` style in `App.css` matches the existing `.job-row__kind`/`.job-row__status` badge conventions. Since both the dashboard list and the job detail page already reuse `JobRow`, this surfaces it in both places from one change — no page-level edits needed. New `frontend/src/components/JobRow.test.tsx` (2 cases: badge renders when `project_id` is set, no badge/no stray element when `null`) — `JobRow` had no dedicated test file before this. `npm run test` → **55 passed** (53 prior + 2 new). `npm run build` clean, `npm run lint` clean (2 pre-existing warnings in unrelated files unaffected). No backend changes needed. Deliberately out of scope: a project-based filter/grouping control (mirroring `StatusFilter.tsx`) — display only, per the task's stated scope; still open in Phase 2 above if wanted later.
- [x] **`storyboard.py` split into four modules by responsibility (2026-07-18, Phase 3):** the 653-line file (the largest in the codebase, roughly a third of all backend code) did routes, shot-splitting/workflow-templating business logic, SSE streaming, and artifact metadata all at once. Zero behavior change — planned via Plan mode with a dedicated Plan-agent design review of the real source and every referencing test file before any code moved, which caught a real gap: the initial estimate that only `tests/test_recovery.py`'s `comfyui_client` references needed updating was corrected to the full ~33-occurrence shot-reconciliation section, since three of its patches (`get_settings`, `get_job_store`, `_load_workflow`) are module-level name rebindings, not object-attribute mutations — those only take effect on whichever module the patched function actually resolves the name from at call time, so they had to move with `_run_storyboard_job` to keep those tests actually isolated from the real DB/filesystem rather than silently starting to leak into it.
  - New `backend/core/storyboard_engine.py` — `_naive_shot_split`, `_load_workflow`, `_apply_shot_to_workflow`, `_save_history_outputs`, `_run_storyboard_job`, the `comfyui_client` singleton, and the `worker.register_handler("storyboard", ...)` call. No FastAPI import, matching every other `backend/core/*.py` file's style.
  - New `backend/core/artifacts.py` — `_artifact_from_path` only.
  - New `backend/api/job_responses.py` — `_build_job_status_response`. Needed as its own module (beyond the four named responsibilities) specifically to avoid a circular import: `storyboard.py` needs `_job_events_stream` from the new `sse.py`, and `_job_events_stream` needs `_build_job_status_response` — keeping the latter in the routes file would make `storyboard.py` and `sse.py` import each other. Verified only `_job_events_stream` (not `_all_jobs_events_stream`/`_job_logs_events_stream`) actually needs it.
  - New `backend/api/sse.py` — `_SSE_KEEPALIVE_SECONDS`, `_sse_frame`, `_all_jobs_events_stream`, `_job_events_stream`, `_job_logs_events_stream`.
  - `backend/api/routes/storyboard.py` trimmed from 653 to 231 lines — now only `router = APIRouter(...)` and the ten `@router.*`-decorated endpoint functions, importing what each needs from the four modules above. No longer imports `ComfyUIClient`/`ComfyUIError`/`Settings`/`JobCancelled` at all (nothing left in the routes file uses them).
  - **Test updates** (no test behavior/assertions changed, only import paths): `tests/test_storyboard_helpers.py`'s import moved to `backend.core.storyboard_engine`; `tests/test_job_routes.py`'s 3 `_artifact_from_path` call sites now import it from `backend.core.artifacts` directly (the only symbol in that file with no remaining caller inside the routes module); `tests/test_recovery.py`'s shot-level reconciliation section (all ~33 `storyboard_module` references, lines ~229-493) repointed to a new `storyboard_engine_module` import — its job-level routing tests (lines ~34-227) were untouched, since they never referenced the storyboard module at all. Everything else that any test referenced via `storyboard_module.X` (route functions, `worker`, the three SSE stream generators) needed **zero test changes**, since `storyboard.py` still genuinely imports each of them to wire up its own routes — not a compatibility shim, a real dependency.
  - **Verification:** `python -c "from backend.app import app"` confirmed the whole import chain resolves with no circular import before running the suite. `python -m pytest tests/ -v` → **128 passed** — identical count to before the refactor, confirming no behavior changed. `npm run test` (frontend, unaffected by this backend-only change) → 55 passed, re-run as a sanity check.
- [x] **Retry/cancel attempt history — backend (2026-07-19, Phase 2):** planned design-first, no code until approved (design doc: `C:\Users\hokph\.claude\plans\agile-whistling-firefly.md`), including a dedicated Plan-agent review before implementation. The design narrowed this item's original framing here ("a genuinely new append-only events table touching every existing state-transition call site") after tracing every write path in `job_store.py`: `_update_job_status_sync`'s `COALESCE` semantics mean it can only ever add information, never erase it — of every write method, only `_reset_job_for_retry_sync`/`_reset_shots_by_status_sync` ever null out a previously-set column, and both are called from exactly one place, `retry_job` in `storyboard.py`. Cancellation itself destroys nothing; a cancelled job's data is only ever at risk if later retried through that same path. So the fix only needed to capture the row `retry_job` is about to overwrite, at the moment it overwrites it — not a general ledger of every transition.
  - New `job_attempts` table in `job_store.py`'s `_SCHEMA` (`id, job_id, status, error, started_at, finished_at, recorded_at`, no `FOREIGN KEY` — matches `shots`/`job_logs`'s existing house style, and nothing anywhere in this codebase ever deletes a `jobs` row). `_reset_job_for_retry_sync` now does one `SELECT` + `INSERT` + the existing reset `UPDATE`, all under the one `commit()`/lock this method already had — a genuine first for this file (every other `_*_sync` method does exactly one statement) but not a risk, and the **public `reset_job_for_retry()` signature is unchanged**, so `retry_job`'s existing call site needed zero edits. New `get_job_attempts(job_id)` (mirrors `get_job_logs` exactly). New `JobAttemptEntry` schema and `GET /api/jobs/{job_id}/attempts` route (mirrors `GET /api/jobs/{job_id}/logs`, same 404 convention). No new SSE stream — attempts change at most once per retry, far rarer than logs/progress, and the existing `job_updated` event on `/jobs/{id}/events` already signals exactly when a client should re-fetch (same "signal only, client re-fetches REST" pattern the dashboard's all-jobs stream already uses).
  - **Deliberately job-level only, no shot-level snapshot table.** A `FAILED` shot's `files` is always `NULL` already (only a `DONE` shot ever gets `files`, nothing re-marks a `DONE` shot `FAILED`) — nothing to lose there. `prompt_id` is recoverable today because every current shot-failure error message happens to embed it as text — real, but a coincidental property of current error-message formatting, not a structural guarantee; named here explicitly as a fragile assumption rather than asserted as solid, matching this file's existing convention for named scope limits.
  - Tests: 5 new in `tests/test_job_store.py` (archives a failed attempt correctly; archives a cancelled attempt too; multiple retries accumulate multiple rows in order; empty for a job never retried; scoped correctly per job id) and 3 new in `tests/test_job_routes.py` (404 on missing job; empty list before any retry; correct shape after a real retry). No existing test needed updating — both pre-existing `reset_job_for_retry`/`reset_shots_by_status` tests assert only on the destination row's shape, which is unchanged. `python -m pytest tests/ -v` → **136 passed** (128 prior + 8 new).
- [x] **Retry/cancel attempt history — frontend (2026-07-19, Phase 2):** the display half of the feature above, following the same existing-pattern inspection the user asked for first (`api.ts`, `types.ts`, `useJobLogs.ts`, `JobExecutionLogs.tsx`, `JobDetailPage.tsx`). New `frontend/src/types.ts::JobAttemptEntry` (matches the backend schema field-for-field, confirmed by direct comparison). New `getJobAttempts(jobId)` in `api.ts`. New `frontend/src/useJobAttempts.ts` — deliberately REST-only, no SSE (attempts change at most once per retry, matching the backend's own reasoning for skipping a dedicated stream), same fetch-and-keep-last-known-on-failure resilience as `useJob`/`useJobList`/`useJobLogs`. New `frontend/src/components/JobAttemptHistory.tsx` — empty state ("No previous attempts."), else a list showing attempt number, status, started/finished times (via existing `formatTime.ts`), and the error message if present; new `.job-attempts*` CSS classes in `App.css` mirror `.job-logs*`'s existing structure/status-coloring convention rather than inventing a new one. Rendered on `JobDetailPage.tsx` between Execution Logs and Artifacts. Tests: new `JobAttemptHistory.test.tsx` (3 cases — empty state, a failed attempt renders status+error, a cancelled attempt renders with no error element), mocking `getJobAttempts` via the same `vi.mock('../api', ...)` + `vi.importActual` pattern `useJob.test.tsx` already uses. `npm run test` → **58 passed** (55 prior + 3 new). `npm run build` clean, `npm run lint` clean (2 pre-existing warnings in unrelated files unaffected). Reviewed against `git diff` before commit: no unrelated files touched, API path and TypeScript types confirmed to match the backend exactly, no accidental formatting changes to surrounding code.
- [x] **Backend test coverage measurement added (2026-07-19, Phase 3):** `pytest-cov` added to `requirements.txt`'s Dev/test section (installed, `coverage==7.15.2` as its resolved dependency) — no application code changed, purely test tooling. New `.coveragerc` (`source = backend`, `show_missing = True`) scopes measurement to the backend package only (tests themselves aren't meant to be measured). `.gitignore` gained `.coverage`/`htmlcov/` for the generated, ephemeral report artifacts. Deliberately **not** wired into `pytest.ini`'s default `addopts` — a plain `pytest`/CI run stays exactly as fast and quiet as before; coverage is opt-in via `pytest --cov=backend --cov-report=term-missing tests/`, documented here as the reproducible command rather than silently changing every future test run's output. Regenerated `requirements.lock.txt` to include the new dependency (`pip freeze` via bash rather than PowerShell this time, which as a side effect fixed a pre-existing, previously-unnoticed UTF-16 encoding on that file from when it was first generated in Phase 1 — confirmed harmless in practice, since `pip install -r` parses it fine either way, but UTF-8 is the more conventional encoding and was a one-line fix while already regenerating the file).
  - **Result: 85% overall** (893 statements, 130 missed), `python -m pytest --cov=backend --cov-report=term-missing tests/ -v` → **136 passed**. Full per-file breakdown, highest-value ones: `job_store.py`/`schemas.py` 100%, `artifacts.py` 100%, `recovery.py`/`config.py`/`events.py` 93-96%, `storyboard.py` (routes) 90%, `worker.py` 90%, `storyboard_engine.py` 85%, `sse.py` 84%. Lowest: `llm_client.py` 52%, `comfyui_client.py` 53%, `gpu_memory.py` 56%, `app.py` 67%, `generate_script.py` 68% — all for the same, already-well-understood reason: their uncovered branches need a live Ollama/ComfyUI instance or real process lifecycle to exercise (this project's established pattern is live-validation for exactly these paths, not more mocks — see the Session Log's many live-validation write-ups). Not a newly discovered gap, just the first time it's been quantified rather than just known qualitatively.
  - Frontend coverage (`vitest --coverage`) is a separate, still-open item — not part of this pass.
- [x] **Backend coverage raised from 85% to 91.27% (2026-07-19, Phase 3):** tests only, per the task's explicit constraint — zero application behavior changed, confirmed by every new/changed test file being under `tests/`. Prioritized "pure logic testable without a live Ollama/ComfyUI/GPU" over the genuinely hard-to-reach lines (real WebSocket internals in `comfyui_client.py`, GPU-optional `gpu_memory.py`, real process lifecycle in `app.py`), which were named and left alone rather than forced.
  - **Two files had zero direct tests at all before this**, despite being core to the app: new `tests/test_llm_client.py` (6 cases — success, `<think>`-block stripping, empty-after-stripping, `APIConnectionError`/`APIStatusError` → `LLMError`, exact user-message construction) covers `backend/core/llm_client.py`'s `LLMClient.generate_script` end to end via a hand-rolled fake swapped in at the `_client.chat.completions.create` boundary (matching `test_comfyui_client.py`'s existing "monkeypatch one bound method on a real instance" convention, not a mocking library) — **52% → 100%**. New `tests/test_generate_script_route.py` (3 cases) covers `backend/api/routes/generate_script.py`'s job handler and the `POST /api/generate-script` route itself, which had never been called by any test — **68% → 100%**.
  - `tests/test_job_routes.py` (+11 cases): `create_storyboard` (the actual `POST /api/storyboard` route) had **zero direct tests** despite being the other core endpoint — added script-splitting, explicit-shots, empty-script-rejected, and too-many-shots-rejected cases. Plus the three streaming routes' (`job_events`/`job_logs_events`/`all_jobs_events`) success-path return value (each previously only tested for their 404 rejection, never confirmed to actually return a `StreamingResponse` for a real job), and `get_job_status`'s own missing-job 404 (tested indirectly everywhere else, but never this exact case) — `backend/api/routes/storyboard.py` **90% → 100%**, `backend/api/job_responses.py` **95% → 100%**.
  - `tests/test_config.py` (+2): direct property tests for `comfyui_http_url`/`workflow_path`, two one-line properties nothing had exercised directly — `backend/core/config.py` **95% → 100%**.
  - `tests/test_storyboard_helpers.py` (+5): `_load_workflow` (real file read + not-found error) had never been tested directly — every existing test monkeypatches it out to a stub. Plus `_save_history_outputs`'s two skip branches (a non-list `node_output` value, an entry missing `filename`) and its happy-path download, previously only exercised indirectly.
  - `tests/test_recovery.py` (+1): `_run_storyboard_job` raising when ComfyUI's health check fails up front — the one guard-clause branch not already covered by the existing shot-reconciliation tests (which all assume a reachable ComfyUI).
  - `tests/test_worker.py` (+3): `SingleSlotWorker.start()`/`.stop()`/`.resubmit()` had never been called directly anywhere — every other test drives the queue via a `_run_one_iteration` helper, and `resubmit()` is always monkeypatched out in route tests. Covers idempotent `start()`, task cleanup on `stop()`, and `resubmit()`'s plain enqueue — `backend/core/worker.py` **90% → 100%**.
  - **Deliberately left alone, named rather than chased**: `backend/core/comfyui_client.py` (53%, real WebSocket/HTTP client internals beyond what's already mocked), `backend/core/gpu_memory.py` (56%, genuinely GPU/torch-optional), `backend/app.py` (67%, real lifespan/process-startup logic), `backend/api/sse.py`'s three keepalive-timeout branches (84%, would need simulating `asyncio.wait_for` timeouts), `backend/core/storyboard_engine.py`'s progress-callback/timeout/finally-drain branches (91%, doable but needs a more elaborate fake `wait_for_completion` than was justified for the last ~2% margin), `backend/core/recovery.py`'s `asyncio.create_task` dispatch line (96%), and `backend/core/events.py`'s 2-line `except asyncio.QueueEmpty` branch — confirmed genuinely unreachable in practice (single-threaded asyncio means nothing can run between the `.full()` check and the immediately-following `.get_nowait()` two lines later), not worth a contrived test.
  - **Result:** `python -m pytest --cov=backend --cov-report=term-missing tests/ -v` → **164 passed** (136 prior + 28 new), **91.27% overall** (893 statements, 78 missed, up from 130). `python -m pytest tests/ -v` (plain, matching CI's exact invocation) also confirmed green.
- [x] **Frontend coverage measurement added (2026-07-19, Phase 3):** `@vitest/coverage-v8` added as a devDependency (`^4.1.10`, version-matched to the already-installed `vitest`) — no application code changed, purely test tooling, mirroring the backend `pytest-cov` session exactly. `frontend/vite.config.ts`'s existing `test` block gained a `coverage` section (`provider: 'v8'`, `reporter: ['text', 'html']`, `include: ['src/**/*.{ts,tsx}']`, excluding test files/`main.tsx`/`vite-env.d.ts`). `.gitignore` gained `frontend/coverage/` for the generated report. Deliberately **not** wired into `package.json`'s default `"test"` script — same opt-in reasoning as the backend: a plain `npm run test`/CI's frontend job stays exactly as fast as before; coverage is `npm run test -- --coverage`.
  - **Result: 66.94% overall** (242 statements, 162 covered) — `Test Files 11 passed, Tests 58 passed`. Lower than the backend's 91% for a real, now-quantified reason rather than a new problem: this frontend's established convention (see nearly every prior Session Log entry) has always been to unit-test pure logic in isolation (`formatTime.ts`, `formatBytes.ts`, `buildTimelineEvents`, `classifyArtifact`) and DOM-render-test a handful of data-bearing components (`JobExecutionLogs`, `JobArtifacts`, `JobAttemptHistory`), while page-level shells and forms have never been rendered in a test at all. Confirmed via the HTML report (istanbul's text reporter hides 100%-covered files from its table by default, which initially looked like missing files until cross-checked): **100% covered** — `formatTime.ts`, `formatBytes.ts`, `useJobAttempts.ts`, `useJobLogs.ts`, `JobAttemptHistory.tsx`, `JobExecutionLogs.tsx`. **Partially covered** — `useJob.ts`/`useJobList.ts` (96%+), `api.ts` (77%), `JobArtifacts.tsx` (90%), `JobTimeline.tsx` (74%, only its pure `buildTimelineEvents` is tested, not the render path). **0% covered, never rendered in any test** — `App.tsx`, `JobList.tsx`, `JobRow.tsx` (only 30% — its dedicated test file covers just the `project_id` badge, not the rest of its render logic), `ScriptForm.tsx`, `StoryboardForm.tsx`, `StatusFilter.tsx`, `DashboardPage.tsx`, `JobDetailPage.tsx`. `types.ts` shows 0% but is type-only (no executable statements) — not a real gap.
  - **Deliberately out of scope**, named rather than chased: raising any of the above numbers. The task was "add coverage measurement," matching the backend session's own precedent of reporting first and treating "close the gaps" as a separate, later decision.
  - **Verification:** `python -m pytest tests/ -v` → 164 passed (backend, unaffected, re-confirmed). `npm run test` (plain) → 58 passed. `npm run build` → clean. `npm run lint` → clean except the same 2 pre-existing warnings (`JobTimeline.tsx`, `JobArtifacts.tsx`) already named in earlier entries.
- [x] **Job/log retention policy — Phase 4, first item (2026-07-19):** started on `feature/v1.3-planning`. Of Phase 4's four named items, only "anything multi-user/remote-access related" actually requires revisiting BUG-3's no-auth/localhost-only decision — retention is pure internal engineering with no security/scope implications, confirmed with the user via `AskUserQuestion` before picking it as the first item to scope.
  - **`backend/core/config.py`**: new `job_retention_days: int = 0` (0 = disabled — this permanently deletes job history and output files, so it's opt-in, not silently active). `.env.example` documents it.
  - **`backend/core/job_store.py`**: new `prune_old_jobs(older_than_days)` — deletes `job_logs`/`job_attempts`/`shots`/`jobs` rows for jobs whose `status` is `done`/`failed`/`cancelled` (never `queued`/`running`/`resuming`/`cancelling`, regardless of age) and whose `created_at` is older than the cutoff. Returns the pruned job ids so the caller can also remove their output files — `JobStore` itself has no filesystem knowledge, matching how `recovery.py` already keeps DB state separate from ComfyUI/filesystem concerns.
  - **New `backend/core/retention.py`**: thin orchestrator (mirrors `recovery.py`'s shape) — calls `prune_old_jobs`, then `shutil.rmtree(ignore_errors=True)`s each pruned job's `output/<job_id>/` directory. No-ops immediately if `job_retention_days <= 0`.
  - **`backend/app.py`**: calls the retention pass in `lifespan`, right after `resume_incomplete_jobs` — confirmed with the user (`AskUserQuestion`) to run **startup-only**, not also as an on-demand endpoint (simpler, matches `recovery.py`'s existing pattern, no new no-auth route). Disjoint from crash recovery by construction (recovery only touches non-terminal jobs, pruning only touches terminal ones), so ordering between the two doesn't matter.
  - Tests: 4 new in `tests/test_job_store.py` (prunes only old+terminal jobs; never touches non-terminal jobs regardless of age; cascades to shots/logs/attempts; returns `[]` when nothing's eligible) and 4 new in `tests/test_retention.py` (no-op when disabled; deletes DB rows + output dir when enabled; leaves recent/non-terminal jobs and their dirs alone; tolerates a missing output dir without raising). `python -m pytest tests/ -v` → **172 passed** (164 prior + 8 new). `npm run test`/`build`/`lint` unaffected (backend-only change), re-confirmed green.
- [x] **PR #1 merged into `master` (2026-07-19).** User confirmed via `AskUserQuestion`: merge now, given v1.2's roadmap is fully closed and CI-verified. Merged via a local fast-forward (`master` was a clean ancestor of `feature/v1.2-development`) and pushed, since no `gh`/token was available to use GitHub's merge API directly — GitHub auto-detects the matching commit history and marks the PR merged. **`ci.yml`'s `push:` trigger deliberately left unchanged** (also confirmed via `AskUserQuestion`) — master-only triggers plus a PR-for-CI-and-review pattern is being kept as the intended convention, not a gap.
- [x] **CI-trigger gap resolved (2026-07-19): PR #1 opened, first real CI run on this branch is green.** Following up on the gap found earlier in the day (this branch's 14+ commits had only ever been verified locally, never via a real GitHub Actions run, since `ci.yml` only triggers on `push`/`pull_request` to `master`), the user opened PR #1 (`feature/v1.2-development` → `master`, https://github.com/hokphaymany2025-oss/manytv-ai/pull/1). Its `pull_request`-triggered run (id `29686401582`, head `ecf6199`) is **the first real GitHub Actions execution this branch has ever had**, and it's green: both the `backend` job (`pytest --cov=backend --cov-report=term-missing tests/ -v`) and `frontend` job (`npm run test -- --coverage`, `npm run build`, `npm run lint`) completed successfully — confirming the coverage-reporting steps added earlier this session genuinely work in a live run, not just locally.
- [x] **README architecture tree refreshed (2026-07-19):** the documented tree predated `job_store.py`, `recovery.py`, `events.py`, `llm_client.py`, `storyboard_engine.py`, `artifacts.py`, `sse.py`, `job_responses.py`, `frontend/`, `tests/`, `pytest.ini`, `.coveragerc`, `requirements.lock.txt`, `requirements-gpu.txt` — all added across 10+ milestones since the tree was last accurate. Also refreshed the Endpoints table (list-jobs, logs, attempts, and the three SSE routes were all missing), the CORS section (now reflects the frontend origin already being allowlisted), and the Frontend section (routing, SSE-backed hooks, test counts). Doc-only change, no code touched. Committed as `6311cff`.
- [x] **Coverage reporting wired into CI (2026-07-19):** the last remaining named-optional Phase 3 item. `.github/workflows/ci.yml`'s backend job now runs `python -m pytest --cov=backend --cov-report=term-missing tests/ -v` (was `pytest tests/ -v`); the frontend job now runs `npm run test -- --coverage` (was `npm run test`). Both tools (`pytest-cov`, `@vitest/coverage-v8`) were already installed from the earlier coverage-measurement sessions — this only changes the CI invocation to surface the report in build logs. **Deliberately report-only, no enforced threshold** (`--cov-fail-under` not set on either side) — the goal was visibility, not a new way for CI to fail; chasing a specific number is a separate, not-yet-requested decision, matching this project's established practice for coverage work. Verified locally with the exact CI commands before changing the workflow file: backend 164 passed, 91% overall; frontend 58 passed, 66.94% overall — both reports render correctly (no config drift between local and CI invocation). `npm run build`/`npm run lint` also re-confirmed clean.

- [x] **Real ComfyUI `/interrupt` support — Phase 4, second item (2026-07-19):** immediate (not just between-shot) cancellation of a mid-generation shot, on `feature/v1.3-planning`. Planned via a design pass first (verified against the actual installed ComfyUI's `server.py`/`execution.py`/`comfy_execution/jobs.py` at `D:\NewProjects\ComfyUI`, not general docs), which surfaced real findings before any code was written:
  - **The right endpoint is `POST /api/jobs/{prompt_id}/cancel`, not the legacy global `POST /interrupt`.** The newer endpoint (`interrupt_if_running`) is atomic under ComfyUI's own queue mutex; the legacy one does a non-atomic prompt_id pre-check before an unconditional global interrupt, which can race a prompt that finishes in between.
  - **An interrupted prompt sends a distinct `execution_interrupted` WS message** — `wait_for_completion` had no handling for it at all, so it silently fell through to the terminal "executing, node=None" event (confirmed to fire unconditionally on success/failure/interrupt alike) and returned whatever `/history` held as if it were a real success.
  - **ComfyUI's `/history` status doesn't distinguish "interrupted" from "genuinely failed"** (`completed: False` either way) — and this same ambiguity was already a **latent, pre-existing gap** in the WS-drop polling fallback (BUG-1's path) and the SUBMITTED-shot resume-after-restart reconciliation, both of which only ever see `/history`, never the WS stream, and neither previously checked `status.completed` at all. Folded the fix in (confirmed via `AskUserQuestion` given it touches production code beyond the new feature's own scope) rather than leaving it for later, since leaving it unfixed would make the new interrupt feature itself silently misbehave under a WS-drop.
  - **`backend/core/comfyui_client.py`:** new `ComfyUIInterrupted(ComfyUIError)`; new `cancel_prompt(prompt_id) -> bool` (`POST /api/jobs/{prompt_id}/cancel`); `wait_for_completion` gains an `execution_interrupted` branch; new module-level `_raise_if_history_incomplete(history, prompt_id)` wired into `_poll_history_until_done` and `storyboard_engine.py`'s resume-reconciliation branch (not into `get_history()`/the WS success path — provably unreachable there once `execution_interrupted` is handled, and `_poll_history_until_done`'s existing "any ComfyUIError = keep polling" loop would otherwise mistake a real completed-but-failed history for "not present yet").
  - **`backend/core/storyboard_engine.py`:** new `request_shot_interrupt(job_id)` — best-effort, finds the currently-`SUBMITTED` shot and calls `cancel_prompt`, swallowing a ComfyUI-unreachable/rejected error (the existing between-shot cooperative check remains the correctness fallback regardless). New `except ComfyUIInterrupted` branch in `_run_storyboard_job`, ordered before the generic `except Exception`: re-checks the job's own persisted status at the moment of the exception — `CANCELLING` → mark the shot `FAILED` (reusing the existing enum, so `/retry`'s `reset_shots_by_status` needs no changes) and raise `JobCancelled` (job ends `CANCELLED`); not `CANCELLING` (e.g. someone hit ComfyUI's own stop button) → mark `FAILED` and re-raise (job ends `FAILED`, unchanged from today for a genuine failure). The resume-reconciliation branch's found-but-incomplete history is treated identically to "no history found" — resubmit, since there's nothing valid to reuse either way.
  - **`backend/api/routes/storyboard.py`:** `cancel_job` calls `request_shot_interrupt` right after setting `CANCELLING`, only for `kind == "storyboard"` — single narrow import, matching the Phase 3 refactor's discipline of not reintroducing `ComfyUIClient`/`Settings` into the routes file.
  - **Tests:** new `tests/test_cancellation.py` (16 cases) covering all three layers — `comfyui_client.py` (`cancel_prompt` success/no-op/unreachable, `execution_interrupted` → `ComfyUIInterrupted`, `_raise_if_history_incomplete` via `_poll_history_until_done` for absent-status/complete/incomplete), `storyboard_engine.py` (`request_shot_interrupt`'s three cases, `_run_storyboard_job`'s CANCELLING-vs-not branches — the CANCELLING case built by flipping the job's status *during* the mocked `wait_for_completion` call, to genuinely simulate the race rather than short-circuiting on the shot loop's existing top-of-iteration check — and the resume-reconciliation resubmit-on-incomplete-history case), and the `cancel_job` route (calls `request_shot_interrupt` for a `SUBMITTED` shot, no-ops when none exists). Also fixed a real test-isolation gap surfaced while writing these: `tests/test_job_routes.py`'s existing cancel tests were silently reaching the *real* global `JobStore` via `request_shot_interrupt`'s own `get_job_store()` name (a separate module-level binding `storyboard_module`'s patch doesn't cover — the same class of gap the Phase 3 refactor's own notes already named) — fixed by patching `storyboard_engine_module.get_job_store` there too. `python -m pytest tests/ -v` → **188 passed** (172 prior + 16 new).
  - **Not live-validated** (needs a real running ComfyUI and a real mid-generation cancel to confirm the shot aborts in seconds rather than waiting for `GENERATION_TIMEOUT_SECONDS`) — matching this project's established practice (see BUG-1's history), that's the natural follow-up once this lands, not a blocker to calling the code done.
- [x] **Mid-run `generate_script` cancellation — Phase 4, third item (2026-07-19):** closes Phase 4's last named engineering item, on `feature/v1.3-planning`. `generate_script`'s single blocking LLM call (`await self._client.chat.completions.create(...)`) has no per-chunk checkpoint to hook a cooperative check into the way storyboard's shot loop does — the only way to actually stop it mid-flight is an asyncio-level task cancellation.
  - **`backend/core/worker.py`:** the handler call inside `_run()`'s loop now runs as its own wrapped `asyncio.Task` (`asyncio.ensure_future(handler(job))`) instead of a bare `await`, so a specific job's execution can be cancelled independently of the worker's own outer loop task. New public `cancel_current_job(job_id) -> bool` — cancels the in-flight task only if `job_id` matches whichever job is actually executing right now; returns `False` (not an error) otherwise.
  - **A real, non-obvious bug found and fixed mid-implementation, not just designed around in advance:** the first version disambiguated "this job's own task was cancelled" from "the worker's own outer loop task was cancelled (`worker.stop()`)" by checking `task.cancelled()` — this hung `worker.stop()` completely (a real deadlock, caught by a test timing out, not by reasoning alone) because `asyncio.Task.cancel()` automatically propagates to whatever a task is currently suspended on (its `_fut_waiter`) — since `_run()` awaits the inner task directly, cancelling the *outer* task also cancels the *inner* one as an automatic side effect, making both scenarios produce an identical `task.cancelled() == True`. Fixed by tracking explicit intent instead: a new `self._cancel_requested_for: Optional[str]` flag, set by `cancel_current_job()` immediately before it calls `.cancel()`, checked in `_run()`'s except block rather than inferring anything from the task's own cancelled state.
  - **`backend/api/routes/storyboard.py`:** `cancel_job`'s old unconditional 409-rejection for a running `generate_script` job is replaced with the same CANCELLING-first structure `storyboard` jobs already use — sets `CANCELLING`, then calls `worker.cancel_current_job(job_id)` for `kind == "generate_script"` (mirroring `request_shot_interrupt` for `kind == "storyboard"`). A `RESUMING`-but-not-yet-dequeued job (rare race) is still handled correctly with zero extra code, since the worker's existing dequeue-time CANCELLING check is already kind-agnostic.
  - Tests: `tests/test_worker.py` (+4: the outer-vs-inner cancellation disambiguation, rewritten from a now-inapplicable old test that simulated outer-task cancellation by having a handler directly `raise asyncio.CancelledError()` — under the new per-job-task architecture that no longer distinguishes from a legitimate targeted cancel, so it had to actually cancel the outer task to correctly exercise the real code path; plus 3 new `cancel_current_job` cases). `tests/test_job_routes.py` (the old `..._is_rejected` test replaced with `..._sets_cancelling`, plus a new test confirming `cancel_current_job` is actually called). `python -m pytest tests/ -v` → **192 passed** (188 prior + 4 net new).
  - `README.md`'s endpoint table updated — both halves of the old "not mid-generation" / "generate_script can't be cancelled" claim were stale after this and the `/interrupt` work above.
  - **Not live-validated** against a real long-running Ollama call, matching the `/interrupt` item above — the mocked/unit-level behavior is confirmed correct, but a real mid-generation cancel against a real qwen3 "thinking" response hasn't been observed yet.

## Current tasks [ ]

- [ ] **Phase 4 engineering items all done on `feature/v1.3-planning`, not yet merged to `master`.** Retention policy, real ComfyUI `/interrupt` support, and mid-run `generate_script` cancellation are all implemented and tested (192 passing). **Anything multi-user/remote-access related remains explicitly gated** on the user revisiting BUG-3's no-auth/localhost-only scope decision before any design work begins there. Still open, unrelated to gating: live-validating both the `/interrupt` and the `generate_script` cancellation features against real ComfyUI/Ollama instances, and deciding when to merge this branch into `master`.

---

## Bugs

### ~~BUG-1 (P0) — ComfyUI WebSocket connection dies mid-storyboard-job with "keepalive ping timeout", losing the whole job~~ — CLOSED 2026-07-17, live-validated

**Confirmed in `server.log`, not hypothetical.** Job `fb4850f6-17c8-4798-881f-6321b45413b5`: shot 0 succeeded fully; shot 1 was queued and then, ~50s later with no progress events logged, the client raised:
```
websockets.exceptions.ConnectionClosedError: sent 1011 (internal error) keepalive ping timeout; no close frame received
```
from `backend/core/comfyui_client.py:70` (inside `wait_for_completion`). The existing mitigation (`ping_interval=None` on the client's own `websockets.connect()`, `comfyui_client.py:76`) only disables the **client's** keepalive pings. Reading the installed `websockets==16.1` source confirms `keepalive()` only runs `if self.ping_interval is not None` — so this exact error can't be coming from the client. It's most likely **ComfyUI's own WebSocket server** failing its side of the keepalive because ComfyUI's process is synchronously blocked mid-generation and can't service its own ping/pong in time — the same failure mode the code comment already anticipated, just from the direction the fix doesn't cover.

- **Impact:** any multi-shot storyboard job can lose all remaining shots (and be marked fully `FAILED`) the moment ComfyUI is busy long enough to miss its own keepalive — which is likely to recur on longer/heavier generations, not a one-off fluke.
- **Fix applied:** rather than chase ComfyUI's own server-side ping/timeout flags (unconfirmed whether they even exist, and wouldn't cover every disconnect cause), `wait_for_completion` in `backend/core/comfyui_client.py` now catches the WebSocket drop and falls back to `_poll_history_until_done`, which polls `/history/{prompt_id}` every 3s until ComfyUI records a finished entry. This works because the prompt keeps executing inside ComfyUI regardless of whether the monitoring socket stays connected — the generation was never tied to it. A genuinely dead ComfyUI (not just a dropped socket) still fails fast: `get_history` raises a non-`ComfyUIError` (an `httpx` error) in that case, which propagates immediately instead of retrying. Real `execution_error` events from ComfyUI (an actual generation failure, not a connection issue) still raise `ComfyUIError` immediately and are not affected by this fallback.
- **Verified by mocks:** `tests/test_comfyui_client.py` (4 cases) — polling retries until history appears, non-ComfyUI errors propagate immediately (no infinite retry against a dead server), a dropped socket falls back to polling rather than raising, and a real `execution_error` still raises immediately without going through the fallback. `python -m pytest tests/ -v` → 15 passed.
- **Verified live (2026-07-17):** ComfyUI (real Arc A750, XPU backend, confirmed via `/system_stats`) and the ManyTV backend were both started for real. A baseline 3-shot job ran cleanly end-to-end first (job `147c1e4e...`, no regression). Windows Firewall block rules were tested and found to have **no effect on loopback traffic** (`127.0.0.1`→`127.0.0.1` is exempt from normal WFP filtering for regular desktop processes) — confirmed empirically, not assumed, so a throwaway TCP proxy (test infra only, not part of the app) was inserted between the backend and ComfyUI instead. Killing and instantly restarting that proxy process severed the backend's live WebSocket connection while never touching ComfyUI's own process. Result, job `5cb0ab45-a739-4665-84ce-846cae8bc267`, shot 0 (ComfyUI prompt `6de5b744-fef8-486b-b936-7c92d6b1beee`), interrupted at KSampler step 18/20:
  ```
  11:12:33,286 [WARNING] manytv.comfyui: WebSocket to ComfyUI dropped while waiting for
  prompt 6de5b744-fef8-486b-b936-7c92d6b1beee (no close frame received or sent);
  falling back to polling /history -- the job may still be running.
  11:12:33,466  GET /history/6de5b744... 200 OK   (poll 1)
  11:12:36,625  GET /history/6de5b744... 200 OK   (poll 2, ~3.16s later -- matches configured 3.0s interval)
  11:12:39,771  GET /history/6de5b744... 200 OK   (poll 3, history now present)
  11:12:39,880  shot 0 done: 1 file(s) saved to output\5cb0ab45.../shot_000\ManyTV_00009.mp4
  ```
  Job then proceeded to shot 1 normally and finished with `status: done`, both shots' `.mp4` files present, non-empty, and confirmed as valid ISO Media/MP4 (not empty/corrupt placeholders). ComfyUI's own log shows exactly one `"Starting server"` line for the entire session — it was never restarted or interrupted, only the backend's monitoring connection was severed. All four documented failure conditions were checked and none occurred: no lost prompt state, no infinite retry (exactly 3 bounded polls), job was not marked failed despite ComfyUI completing, and (per the existing mocked test, not re-exercised live since no real ComfyUI execution error occurred this session) `execution_error` handling is unaffected by this fallback path.
  - **Scope note:** the live run validates the WebSocket-drop → polling-fallback → job-completes path end-to-end for real. It does not independently re-prove the `execution_error`-still-raises-immediately guarantee live (no real execution error occurred to observe) — that guarantee rests on the mocked unit test only.

### ~~BUG-2 (P1) — `workflow_name` is not sanitized before being used to build a filesystem path~~ — FIXED 2026-07-17

`backend/api/routes/storyboard.py:39-47`:
```python
path = settings.workflow_path / f"{workflow_name}.json"
```
`workflow_name` came directly from the `StoryboardRequest` request body with no validation (no allowlist, no `Path.name`-only check, no rejection of `/` or `..`). A request like `{"workflow_name": "../../../../some/other/file"}` would attempt to read `<other/file>.json` from outside `backend/workflows/`. Combined with wildcard CORS (`allow_origins=["*"]`) and zero authentication on any endpoint (see BUG-3), this was reachable from any origin, including a malicious webpage in a browser on the same machine.
- **Fix applied:** `StoryboardRequest.workflow_name` in `backend/models/schemas.py` now has `pattern=r"^[A-Za-z0-9_-]+$"` on its `Field`, rejecting anything with `/`, `\`, `.`, spaces, or empty string at the Pydantic validation boundary (HTTP 422) before it ever reaches `_load_workflow`. Fixed at the model layer only — every call path into `_load_workflow` goes through this already-validated field, so no redundant check was added inside `_load_workflow` itself.
- **Verified by:** `tests/test_schemas.py` — 8 traversal/invalid-character cases rejected, 3 valid names (including the `"default_t2v"` default) still accepted. `python -m pytest tests/ -v` → 11 passed.
- **Residual risk, tracked separately:** BUG-3 (wildcard CORS + no auth) is what made this reachable from a browser in the first place and is still open — fixing BUG-2 closes this specific input, not the exposure model.

### ~~BUG-3 (P1) — No authentication and wildcard CORS on every endpoint~~ — CLOSED 2026-07-17

`backend/app.py:48-53` (pre-fix): `allow_origins=["*"]`, `allow_methods=["*"]`, `allow_headers=["*"]`, and no auth dependency on any route. This is exactly what turned BUG-2 into something exploitable by any page the user's browser visited, not just a deliberate attacker with network access.
- **Scope confirmed with the user before fixing:** this deployment is and will remain localhost-only, single-user (no LAN/remote access planned). That ruled out building a shared-secret/API-key auth layer as unnecessary scope — the fix is CORS + host-binding only, not a new auth mechanism.
- **Fix applied:** `Settings.cors_allowed_origins` (`backend/core/config.py`, env var `CORS_ALLOWED_ORIGINS`) is a comma-separated allowlist defaulting to **empty** — there's no frontend yet, so there's no legitimate cross-origin caller to allow. `backend/app.py`'s `CORSMiddleware` now uses `settings.cors_allowed_origins_list` instead of `["*"]`, and `allow_methods`/`allow_headers` are narrowed to `["GET", "POST"]`/`["Content-Type"]` (the only ones actually used) instead of wildcards. Confirmed live with a `TestClient` preflight from `http://evil.example`: pre-fix this would return `200` with `Access-Control-Allow-Origin: *`; post-fix it returns `400` with no ACAO header, while `/health` (no `Origin` header, i.e. same-machine curl/tooling) still returns `200` normally.
  - Host binding: `uvicorn`'s own default is already `127.0.0.1` (confirmed via `uvicorn --help` against the installed version) — the README's run command never overrode this, so no code change was needed there, but README now calls this out explicitly as a security boundary rather than an incidental default, warning against `--host 0.0.0.0` without adding real auth first.
  - No auth layer was added, by design (see scope note above) — if this backend is ever exposed beyond this one machine, both the CORS allowlist and the host binding need revisiting together with adding real auth, not in isolation.
- **Verified by:** `tests/test_config.py` (new, 3 cases — empty default, comma-split parsing, whitespace/empty-entry handling) plus the live `TestClient` preflight check above. `python -m pytest tests/ -v` → 35 passed.
- **Docs:** README gained a "CORS and network exposure" section, and its stale "Known gaps" section (still described job state as in-memory-only, and the shipped workflow as `default_i2v.json`/missing — both false since the Job Manager milestone and the shipped `default_t2v.json`) was corrected to point at this file instead of re-describing status that drifts.

### ~~BUG-4 (P2) — Partial job failure loses successful-shot results~~ — CLOSED 2026-07-17

`backend/api/routes/storyboard.py`: if shot *N* failed, the whole `_run_storyboard_job` handler raised, `worker.py`'s catch-all set `job.status = FAILED` and `job.error = str(exc)`, but `job.result` was never populated — so shots 0..N-1 that *succeeded* (and whose files were sitting on disk in `output/<job_id>/shot_000/` etc.) were invisible to anyone polling `/api/jobs/{id}`. This directly happened in the one real run on record at the time (BUG-1's job): shot 0's `.mp4` existed but the job API reported only an error, no indication shot 0 worked.
- **Fix applied:** landed as a side effect of the Job Manager + Queue + Resume System milestone rather than as a standalone patch (deliberate sequencing decision — see that milestone's entry above). `GET /api/jobs/{id}` (`backend/api/routes/storyboard.py::get_job_status`) now builds its `result`/`shots` response from the persisted `shots` table at read time, which is populated as shots complete regardless of whether the job later fails — so partial progress is visible immediately, even while the job is still `RUNNING`, not only once it reaches a terminal state.
- **Verified by:** `tests/test_recovery.py` shot-reconciliation tests exercise the same persisted-shots read path; live-validated as part of the resume test (see Session Log) — a `GET /api/jobs/{id}` mid-job showed shot 0 as `done` while shot 1 was still `submitted`.

### BUG-5 (P3) — Two independent `ComfyUIClient` instances constructed for no functional reason

`backend/app.py:31` (lifespan health check) and `backend/api/routes/storyboard.py:31` (module-level) each construct their own `ComfyUIClient(get_settings())`. Harmless (the class is stateless) but redundant — worth consolidating into one shared instance if the class ever grows connection-pooling state.

### ~~BUG-6 (P1) — `JobStore`'s single SQLite connection is not safe under real concurrent access~~ — FOUND & FIXED 2026-07-17

`backend/core/job_store.py`'s module docstring claimed "one long-lived connection in WAL mode needs no extra locking," reasoning that there's "only ever one writer... plus occasional readers." That assumption broke the moment something could issue truly concurrent reads: every public method wraps its sync `sqlite3` call in `asyncio.to_thread`, which dispatches to Python's real thread pool — two calls landing on two different OS threads at the same moment is exactly what a frontend polling several jobs' `GET /api/jobs/{id}` at once produces. `sqlite3.connect(..., check_same_thread=False)` only disables Python's same-thread *check*; it does not make the single shared `Connection` object safe for concurrent use from multiple threads at once. WAL mode is unrelated to this — it governs crash-safety/reader-writer isolation at the file level, not in-process thread-safety of one Python object.
- **How it was found:** not by code review or the 71 mocked tests (none of which exercise true OS-thread concurrency) — it surfaced live, the first time this project had a real client (the new frontend) polling multiple jobs concurrently. Manifested in the browser console as `Access to fetch ... has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present`, which was initially (and incorrectly) suspected to be a CORS misconfiguration — the backend log told the real story: `sqlite3.InterfaceError: bad parameter or other API misuse`, a 500 that (for reasons not fully root-caused, plausibly a raw connection reset bypassing normal header-adding middleware) Chromium reported using its generic cross-origin failure message instead of surfacing the actual 500.
- **Fix applied:** `JobStore.__init__` now holds a `threading.Lock`; the one choke point every public method already funnels through (`_run()`) wraps the actual sync call in that lock before dispatching to the thread pool, so only one thread ever touches `self._conn` at a time. Minimal, single-point fix — no changes needed to any of the individual `_*_sync` methods.
- **Verified by:** new `tests/test_job_store.py::test_concurrent_access_does_not_raise_sqlite_interface_error` (200 concurrent operations across a batch of jobs via `asyncio.gather`) — confirmed this test reliably reproduces the exact real error on every run when the lock is removed (3/3 attempts), and passes cleanly with the fix. Live-reverified: killed and restarted the real backend with the fix, re-ran the same Playwright browser session that had shown the CORS-shaped errors, got zero console errors on both a full run and a repeat.

---

## Missing features

- [x] ~~Job persistence.~~ Done 2026-07-17 — see the Job Manager + Queue + Resume System milestone above (`backend/core/job_store.py`, SQLite).
- [x] ~~Partial-result recovery / resume.~~ Done 2026-07-17, live-validated — same milestone. Note the scope boundary stated there: this covers *process-interruption* recovery (backend killed mid-job); automatic retry-from-last-successful-shot after a *genuine* shot failure (bad prompt, ComfyUI OOM) is still a distinct, unbuilt feature — a `FAILED` job is still terminal and isn't picked up by startup reconciliation.
- [x] ~~Retry a FAILED job from its last successful shot.~~ Done 2026-07-17, live-validated — see the Job retry/cancel/frontend milestone above. Also covers retrying a `CANCELLED` job with partial shot progress (rejected only if it never started, i.e. cancelled while still `QUEUED`).
- [x] ~~Job cancellation.~~ Done 2026-07-17, live-validated — same milestone. Scope limits: a `RUNNING` job only stops between shots (no ComfyUI `/interrupt`), and a `RUNNING` `generate_script` job can't be cancelled at all, only a `QUEUED` one.
- [x] ~~Structured/queryable job history.~~ Done 2026-07-17, live-validated against real multi-session history — `GET /api/jobs` (optional `?status=` filter), see the milestone entry above. The frontend now uses it (see below) — job history is real server-side state, not per-browser `localStorage`.
- [x] ~~Automated tests — still incomplete.~~ Fully closed 2026-07-18: `tests/test_worker.py` (2026-07-17, 9 cases) covers the worker's queue/failure/cancellation semantics directly; `tests/test_storyboard_helpers.py` (2026-07-18, 14 cases) closes the last gap — `_naive_shot_split`/`_apply_shot_to_workflow` in `storyboard.py` now have direct unit tests, not just indirect coverage through `_run_storyboard_job`. See the milestone entry above.
- [x] ~~CI.~~ Done 2026-07-17 — `.github/workflows/ci.yml`, `pytest` on push/PR to `master`, now 72 fast tests, no live services needed. Unpushed repo means it hasn't been observed firing on a real GitHub runner yet (see "Completed" note above); dry-run-validated locally instead. Note: this doesn't touch the lockfile gap below — CI still installs whatever `requirements.txt`'s unpinned floors resolve to on the day it runs.
- [x] ~~Frontend.~~ Done 2026-07-17, live-validated with a real browser (Playwright) — see the Job retry/cancel/frontend milestone above. `frontend/` — React + Vite + TypeScript, a minimal job dashboard. Job list is now real server-side history via `GET /api/jobs`, has a status-filter control, real routing with a Job Detail page (`react-router-dom`, `/jobs/:id`), and now a Job Timeline on the detail page (see below) — all done, see the milestone entries above. No auth-aware UI needed per BUG-3's confirmed scope. No status-filter control on the detail page itself (not asked for).
- [x] ~~Job timestamps in the API + detail page.~~ Done 2026-07-18, live-validated — see the Job Timeline milestone above. `JobStatusResponse`/`ShotStatusResponse` now expose all six persisted timestamps; a new `JobTimeline` component renders them as a chronological event list on the Job Detail page.
- [x] ~~Job output artifacts (rich preview + metadata, no raw path leakage).~~ Done 2026-07-18, live-validated against real ComfyUI-produced video files — see the Job Output Artifacts milestone above. `ShotStatusResponse.files` now returns structured `ArtifactResponse` objects (filename, size, content-type) instead of raw local paths; the Job Detail page renders inline `<video>`/`<img>` previews.
- [ ] **Request size limits.** No length cap on `prompt`/`script` free-text fields. (P3)
- [ ] **Dependency pinning/lockfile.** `requirements.txt` uses floors only (`>=`); no lockfile, so a fresh install on a new machine isn't guaranteed to reproduce the exact verified environment. (P2)
- [ ] **`generate_script` resume is always a full re-run.** Documented, permanent asymmetry (an LLM completion has no ComfyUI-`/history`-style idempotency to check) — not a gap to fix, just worth remembering if it's ever surprising in practice. (informational)
- [x] ~~Retry/cancel attempt history isn't shown anywhere.~~ Backend done (2026-07-19) — see the Completed entry below. Narrower than the original framing here turned out to require: only `retry_job`'s two reset calls ever destroy prior-attempt data, not every state transition, so a full event-sourcing table wasn't needed. Frontend display of the new `GET /api/jobs/{job_id}/attempts` endpoint is not yet built — still open, small.
- [ ] **Per-artifact `stat()` on every job-status poll has no caching.** `GET /api/jobs`'s list view has no pagination and already returns 24+ real accumulated jobs; each now does a filesystem `stat()` per output file every time it's fetched (now SSE-triggered rather than strictly every 3s, but the per-call cost is unchanged). Cheap at this project's current single-user scale, a non-issue today, but an un-cached, un-bounded cost that would need addressing before a much larger job history or a busier deployment. (P3)
- [x] ~~Frontend polling replaced with Server-Sent Events.~~ Done 2026-07-18, live-validated including a real reconnection-after-backend-restart check — see the milestone entry above. All three data-fetching hooks now push-driven instead of 3s-interval-polled; zero new frontend dependency.
- [ ] **SSE's single-process assumption.** The in-memory `EventBus` only works because the backend runs as one `uvicorn` process (confirmed via README's documented run command, no `--workers N`). If that ever changed, a second worker process's writes would be invisible to subscribers connected to a different worker. Named, not a problem for this deployment. (informational)
- [ ] **Browser's 6-connections-per-origin HTTP/1.1 cap.** `EventSource` connections count against it; a non-issue at this project's typical 2-3-concurrent-stream single-user scale, but worth naming if usage patterns ever involve many simultaneous tabs. (informational)
- [ ] **`EventSource` can't set custom request headers.** Irrelevant today (no auth layer, BUG-3's confirmed scope), but would matter if auth were ever added — would need a token-in-URL or cookie-based workaround, a standard SSE+auth wrinkle. (informational)
- [ ] **The SSE `useJob` 404 path is a compromise, not fully clean.** `EventSource` can't expose an HTTP status code once a stream begins, so the clean `ApiError.status === 404` check still happens via one one-shot `fetch` before the stream opens (preserved byte-for-byte), but a job that's deleted *after* a stream is already open (impossible today — nothing deletes jobs) would have no equivalently clean signal. Named for completeness, not currently reachable. (informational)

---

## Recommended next steps (in order)

1. ~~`git init` and commit the current working state as-is~~ — done 2026-07-17.
2. ~~Fix BUG-2 (path traversal in `workflow_name`)~~ — done 2026-07-17.
3. ~~Fix/mitigate BUG-1 (WebSocket keepalive drop)~~ — implemented, unit-tested, and **live-validated against a real ComfyUI instance 2026-07-17**. Closed.
4. ~~Fix BUG-4 (partial job results) + add job persistence~~ — both landed together 2026-07-17 as the Job Manager + Queue + Resume System milestone, live-validated (real backend-crash-mid-job recovery observed). Closed/done.
5. ~~Revisit CORS/auth (BUG-3)~~ — done 2026-07-17. Confirmed scope with the user (localhost-only, single-user, no LAN/remote access planned): CORS now defaults to an empty allowlist instead of `*`, host binding stays `127.0.0.1`, no auth layer added (out of scope for the confirmed deployment model — revisit together if that ever changes).
6. ~~Add CI~~ — done 2026-07-17. `.github/workflows/ci.yml` runs the suite on push/PR; also split `requirements.txt` (torch/XPU moved to `requirements-gpu.txt`, optional). Whether the workflow has actually fired and passed on a real GitHub Actions runner hasn't been directly confirmed yet (no `gh` CLI or web access available in-session) — no longer blocked on missing a remote (see item 12), just not yet independently checked.
7. ~~Job retry, job cancellation, and a first frontend~~ — all done 2026-07-17, live-validated with real curl calls and a real Playwright-driven browser session. Along the way, found and fixed a real pre-existing concurrency bug in `JobStore` (BUG-6) that only a genuine concurrent client (the new frontend) could have surfaced. See the milestone entry in "Completed" for full detail.
8. ~~Expose a list-jobs endpoint~~ — done 2026-07-17, live-validated against real multi-session job history. `GET /api/jobs` (optional `?status=` filter). See the milestone entry in "Completed" for full detail.
9. ~~Update the frontend to use `GET /api/jobs`~~ — done 2026-07-17, live-validated with two independent Playwright browser contexts (proving shared server history, not `localStorage`). Also added the frontend's first automated tests (`vitest`). See the milestone entry in "Completed" for full detail.
10. ~~Status-filter control in the dashboard UI~~ — done 2026-07-17, live-validated against real accumulated history (24 jobs, correctly narrowed to the 6 real failed ones and back). See the milestone entry in "Completed" for full detail.
11. ~~Job Detail page~~ — done 2026-07-17, live-validated (real navigation, back button, hard-refresh SPA fallback, 404 handling, retry from the detail page). First real routing (`react-router-dom`) in this frontend. See the milestone entry in "Completed" for full detail.
12. ~~Push to a real GitHub remote~~ — done 2026-07-18. `origin` → `https://github.com/hokphaymany2025-oss/manytv-ai.git`, `master` pushed along with the `v0.7-job-dashboard`/`v0.8-job-timeline` tags. Confirming the CI workflow actually fires for real (item 6) is the one remaining loose end, not independently checked yet.
13. ~~Extend test coverage~~ to `storyboard.py`'s `_naive_shot_split`/`_apply_shot_to_workflow` pure functions specifically — done 2026-07-18, 14 new direct unit tests, zero production code changes needed (every documented behavior held). See the milestone entry in "Completed" for full detail.
14. ~~Job timestamps~~ — done 2026-07-18, live-validated. See the Job Timeline milestone entry in "Completed" for full detail.
15. ~~Job Execution Logs~~ — done 2026-07-18, live-validated (including confirming logs persist across a retry). See the milestone entry in "Completed" for full detail.
16. **No retention/pruning for `job_logs`** (or `jobs`/`shots`) — new, low priority. `job_logs` will grow faster per job than the other two tables (several narrative events per job vs. one row / N-shot rows); not a problem at this project's current single-user, human-paced scale, but worth naming for whenever that changes. (P3)
17. ~~Job Output Artifacts~~ — done 2026-07-18, live-validated against real ComfyUI-produced video files plus a throwaway-fabricated job for the image/other/missing-file branches. See the milestone entry in "Completed" for full detail.
18. **Un-cached per-artifact `stat()` on every poll** — new, low priority. See the "Missing features" entry above.
19. ~~Replace frontend polling with Server-Sent Events~~ — done 2026-07-18, live-validated including a real backend-restart-mid-connection reconnection test. See the milestone entry in "Completed" for full detail. Found and fixed a real route-registration-order bug (`/jobs/events` vs `/jobs/{job_id}`) via live `curl` testing, not planning or unit tests.
