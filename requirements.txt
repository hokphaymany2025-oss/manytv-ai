# ---- FastAPI orchestration backend ----
# This is the only stack that needs to be installed in ManyTV's own venv.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
websockets>=13.0
python-dotenv>=1.0
python-multipart>=0.0.9
openai>=1.50   # OpenAI-compatible client; talks to local Ollama by default

# ---- Dev / test ----
pytest>=8.0
pytest-cov>=5.0   # coverage measurement -- `pytest --cov=backend --cov-report=term-missing tests/`

# ---- Intel Arc (XPU) torch stack ----
# Optional -- moved to requirements-gpu.txt. Not needed to run this
# backend, its test suite, or CI; see that file for what it's for and why
# it's split out.
